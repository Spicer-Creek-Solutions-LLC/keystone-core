version: "3.9"
services:
  bootstrap-test:
    build:
      context: .
      dockerfile: Dockerfile.ubuntu
    privileged: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    command: ["/lib/systemd/systemd"]
