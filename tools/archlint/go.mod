// archlint checks the requirements register against the invariants, and the
// evidence a completed task owes against what exists.
//
// Its own module, like tools/capcheck and tools/doclint: standard library only,
// and a lifetime independent of the product.
module go.keystone-core.io/keystone-core/tools/archlint

go 1.27
