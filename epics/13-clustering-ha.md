# Epic 13: Clustering & HA — The v1.0 Differentiator

**Phase**: H • **Estimate**: 4 weeks • **Depends on**: 02, 03, 04, 05, 09 • **Blocks**: v1.0 release

## Goal

Run `kscore-server` as a 3-node cluster with leader election, automatic failover, consistent-hash agent assignment, NATS-fallback recovery, and split-brain prevention. **This is the commercial-trial-ready differentiator. Do not defer.**

## Scope (in)

### Topology (v1.0)

- 3 × `kscore-server`, each with embedded etcd (single binary deploy).
- 1 × Postgres (shared state).
- 1 × NATS cluster (or embedded NATS in each server with leaf links — v2.0).
- Production scaling path: 5 × CP + external 3-node etcd + Postgres replica + external NATS cluster.

### Core components (`internal/cluster/`)

- `EtcdClient` — wraps `etcd v3 client`; embedded or external mode; lease + watch; auto-sync.
- `MembershipManager` — ephemeral keys (lease TTL 15s); 5s heartbeat; observers; member metadata; load/watch members.
- `LeaderElector` — `concurrency.Election` primitives; campaign loop; resignation; transfer; observers.
- `ShardManager` — consistent hash ring (default 150 virtual nodes); agent → member by `hash(agentID)`; rebalance on topology change.
- `ShardStore` — etcd-backed `agentID → memberID` mapping, versioned for optimistic locking.
- `HealthMonitor` — consecutive-failure threshold (default 3); P50/P99 latency; partition detection via quorum loss.
- `FailoverManager` — detect failed members → reassign agents (batch 100) → reassign jobs (batch 50, idempotency keys); cooldown 10s.
- `SingletonTaskManager` — leader-only tasks: reactor coordinator (v1.1), scheduled jobs (v1.1), cleanup, metric aggregation, agent rebalance, audit retention enforcer.
- `RecoveryManager` — phases STARTING → CONNECTING → SYNCING → VERIFYING → REJOINING → RECLAIMING → COMPLETED.
- `FencingManager` — lease + epoch fencing; modes STRICT, READ_ONLY, GRACEFUL.
- `CoordinationServer/Client` — mTLS gRPC `CoordinationService` for server↔server; NATS-down recovery channel.
- `StateStore`, `ConfigStore`, `ShardStore` — etcd-backed namespaced stores (general state, hot-reload config, shard map).

### Lifecycle state machines

- Member: `HEALTHY → DEGRADED → UNHEALTHY → LEAVING → removed` (with `recover` arrow back to HEALTHY).
- Leader: `no leader → CAMPAIGNING → ELECTED → (TRANSFERRED|LOST)`.
- Failover: `IDLE → DETECTING → INITIATED → IN_PROGRESS → COMPLETED` (with `FAILED` and `ROLLED_BACK` terminal states).
- Recovery: 7 phases per above.
- Graceful shutdown: `RUNNING → INITIATED → DRAINING → TRANSFERRING → DEREGISTERING → COMPLETED`.

### APIs

- gRPC `ClusterService` (Epic 03 protos): `GetClusterStatus`, `ListMembers`, `GetMember`, `AddMember`, `RemoveMember`, `GetLeader`, `TransferLeader`, `Rebalance`, `CreateBackup`, `RestoreBackup`, `WatchMembership`, `WatchLeadership`.
- gRPC `CoordinationService` (mTLS-only): `ClusterHealth`, `GetLeader`, `NATSStatus`, `RecoveryCoordinate`, `Heartbeat`, `PropagateState`.
- REST: `/api/v1/cluster/{status,members,members/{id},leader,leader/transfer,rebalance,backup,restore}`.
- `kscore-cluster` CLI: `status`, `members`, `leader`, `add`, `remove`, `transfer-leader`, `rebalance`, `backup`, `restore`.
- `kscore-cluster-backup` CLI: `backup`, `restore`, `list`, `verify`, `schedule` (v1.1).

### Cluster backup

- Binary + JSON snapshot; cluster metadata, shard assignments, config; leader-initiated for ordering.

### HA resilience tests (CI on every PR)

- NATS-failure, etcd-failure, network-partition, split-brain.
- Performance SLOs verified: cluster forms <10s; first leader <3s; failover detection <5s; agent reassign <10s; minority blocks writes <1s; recovery <15s.

## Scope (out / non-goals)

- Backup automation/scheduling — v1.1.
- Comprehensive HA dashboard — v1.2 (basic status sufficient for v1.0).
- Read-only replicas — v1.4.
- Auto-scaling — v1.5.
- Multi-region clustering / federation — v2.0.
- Dynamic shard splitting under load — v2.x.
- Advanced topology (gateway / proxy members) — v2.x.

## Design summary

See `PROJECT-DETAILS.md §4.15`.

## Tasks

1. **`EtcdClient` wrapper** — embedded mode with `etcd server/v3` library; external mode connects to existing cluster; auto-sync; lease management.
   _(landed: new **`internal/cluster/`** package + first dep of the epic — `go.etcd.io/etcd/{client/v3,server/v3,api/v3}` v3.6.11 (comparison in the plan: gossip/raw-raft/Postgres-lock alternatives can't meet the quorum/fencing acceptance; PROJECT-DETAILS §3.2 updated). `EtcdClient`: embedded mode runs an in-process `embed.Etcd` (single-member bootstrap — multi-member join is Task 2's MembershipManager), waits `Server.ReadyNotify()` within `StartTimeout`, wires the client via `etcdserver/api/v3client` (no network hop); external mode `clientv3.New` + fail-fast `Status` probe → `ErrEtcdUnavailable`. Single-use lifecycle (created→started→stopped; double-Start→`ErrAlreadyStarted`, Start-after-Stop→`ErrStopped`, Stop idempotent, ops pre-start→`ErrNotStarted`), mutex-guarded. Lease grant/keepalive(worker-ctx, gosec-G118-clean)/revoke (revoke idempotent on unknown lease); thin Put/Get/Delete/Watch/Txn passthrough; `Client()` exposes `*clientv3.Client` for Task 3's `concurrency.Election`. New **`config.ClusterConfig`** (`cluster.enabled` **false by default — opt-in**; embedded|external validation; lease-ttl floor — the heartbeat≥3× rule lands with Task 2). `internal/cluster` 85.7% cov (>80% gate), race-clean; full `make lint`/test/docs-lint green. Tasks 2-18 NOT started — one-task-one-PR.)_
2. **`MembershipManager`** — register on Start with ephemeral lease; heartbeat; LoadMembers; WatchMembers; AddObserver/RemoveObserver.
   _(landed: `internal/cluster/membership.go` on Task 1's `EtcdClient` — **no new dep**. `Member` + `MemberStatus` state machine (`HEALTHY→DEGRADED→UNHEALTHY→LEAVING`, recover edge; validated `canTransition`; Task 2 drives only HEALTHY/LEAVING + a `SetStatus` seam, the DEGRADED/UNHEALTHY edges are HealthMonitor's in Task 7). `Register` grants an ephemeral lease + keepalive + writes `<prefix>/members/<id>` `WithLease`; heartbeat loop (worker-ctx, gosec-G118-clean) refreshes `LastHeartbeat`; `LoadMembers`/`GetMember` (malformed-skip, `ErrMemberNotFound`); single shared watch (`WithPrefix`+`WithPrevKV`) → `MemberEvent{Joined|Updated|Left}` fan-out to `MembershipObserver`s (`AddObserver`/`RemoveObserver`, snapshot dispatch mirroring the RunObserver convention); `WatchMembers(ctx)` = channel adapter over one shared watch, auto-removed + closed on ctx cancel (drops on slow consumer). **Stable member ID** persisted to `MemberIDFile` (UUIDv7; survives restart so RecoveryManager/Task 10 reclaims shards). `Deregister`/`Stop` announces LEAVING then revokes the lease; single-use lifecycle (`ErrNotRegistered`/`ErrAlreadyStarted`/`ErrStopped`, idempotent Stop). **Anti-flap guard (the Task 1 deferral, now closed):** `config.ClusterConfig` gains `membership.{heartbeat_interval,key_prefix}` and `Validate` rejects `lease_ttl_seconds < 3× heartbeat` (risk-list "CI must not allow tighter"); defaults 5s/15s sit exactly at 3×; the `MembershipConfig` runtime mirror enforces it too. `internal/cluster` 84.0% cov (>80% gate), race+lint+docs-lint green. Lease-expiry crash detection deferred to the HA E2E suite (Task 17). Tasks 3-18 NOT started.)_
3. **`LeaderElector`** — `concurrency.Election`; Campaign in goroutine; Resign; TransferLeadership; observers fire on transitions.
   _(landed: `internal/cluster/election.go` on Task 1's `EtcdClient.Client()` — **no new dep** (`go.etcd.io/etcd/client/v3/concurrency` is a client/v3 subpkg). `LeaderState` SM (`Unknown→Campaigning→Elected→{Resigned|Transferred|Lost}→Campaigning`); single campaign-loop goroutine (worker-ctx) owns the `*Session`/`*Election` (no cross-goroutine races): `Campaign` → `Elected`; in-leader `select` on `workerCtx.Done()` (resign+exit) / `session.Done()` (Lost → recreate session → re-campaign) / resign-or-transfer cmd. `Resign` re-enters the queue; `TransferLeadership` = resign + `ReCampaignDelay` so a peer wins; both are no-ops if not leader (no blocking). `IsLeader()`/`State()`/`LeaderID(ctx)` (→`ErrNoLeader`); `LeadershipObserver` fan-out (snapshot dispatch, AddObserver/RemoveObserver) per the MembershipObserver convention. Single-use lifecycle (`ErrNotStarted`/`ErrAlreadyStarted`/`ErrStopped`, idempotent Stop). New `config.ClusterElectionConfig` (`session_ttl_seconds` default 3 = the "<3s leader" SLO target; `recampaign_delay` default 1s; separate from the membership anti-flap lease — SLO tuning is Task 18). **`IsLeader()` is the `func() bool` the `WithRetentionLeaderCheck` seam wants — wiring it (replacing `AlwaysLeader` in internal/audit + internal/events) is Task 9's job, NOT done here.** `internal/cluster` 82.3% cov (>80% gate), race+lint+docs-lint green. Hard-crash session-lease-expiry failover (vs graceful Resign/Stop, both tested) deferred to HA E2E (Task 17) + SLO verification (Task 18). Tasks 4-18 NOT started.)_
4. **Consistent hash ring** + virtual nodes; `Add(member)`, `Remove(member)`, `Get(agentID) → member`.
   _(landed: `internal/cluster/hashring.go` — **pure data structure, no etcd, no new dep** (stdlib `hash/fnv` FNV-1a-64; rejected xxhash/blake as unjustified deps, crc32 as weaker). `HashRing`: `Add`(idempotent)/`Remove`(no-op-if-absent)/`Get`(binary search + wrap)/`Members`/`Has`/`Len`, RWMutex (RLock lookups, Lock topology). **Deterministic rebuild from the sorted member set** → identical key→member mapping on every node regardless of Add/Remove order (required for cluster-wide ownership agreement + Task 6 minimal-migration); vnode-hash collisions resolve to the lexicographically-smallest member, order-independent. New `config.ClusterShardConfig.virtual_nodes` (default 150 per §4.15, `Validate` rejects <1); `NewHashRing(≤0→DefaultVirtualNodes)`. Tests assert stability, order-independence, distribution sanity, and the acceptance criterion — adding a 4th member moves only ~1/4 of keys and **only onto the newcomer** (never reshuffles survivors); remove redistributes only the departed member's keys. `internal/cluster` 84.4% cov (hashring fully covered), race+lint+docs-lint green (pre-existing unrelated `internal/statemgmt/stdlib/service` -race flake is ROADMAP-logged — not in this diff). ShardStore (T5), rebalance+cooldown+ShardManager composition (T6), FailoverManager (T8) NOT started.)_
5. **`ShardStore`** — etcd-backed agentID→memberID with version (optimistic locking).
   _(landed: `internal/cluster/shardstore.go` on Task 1's `EtcdClient` — **no new dep**. Stateless wrapper over a started `EtcdClient` (no own lifecycle/goroutines; `ErrNotStarted` propagates). JSON record `{member_id,updated_at}` at `<key_prefix>/shards/<agentID>`; **`Version` = etcd `ModRevision`** (the CAS token, etcd-owned, not in the value). `Assign` (unconditional upsert via single-op Txn → post-write revision); **`AssignIf(expected)`** optimistic CAS via `EtcdClient.Txn()` (`expected==0` = create-only `Compare(CreateRevision,=,0)`, else `Compare(ModRevision,=,expected)`; mismatch → `ErrVersionConflict`); `Get` (`ErrShardNotFound`); `List` (prefix scan, sorted, malformed-skip+log); `Delete` (idempotent); **`DeleteIf(expected)`** CAS delete (nil if version matches OR key already absent — goal met; `ErrVersionConflict` only when present at a different version); `Watch` thin passthrough (`WithPrefix`+`WithPrevKV`) → `ShardEvent{Set|Deleted}` (Deleted member from prev-kv), ctx-scoped, closes on cancel. New errors `ErrShardNotFound`/`ErrVersionConflict`. No config.go change (shard prefix derives from `cluster.membership.key_prefix`). `internal/cluster` 84.6% cov (>80% gate), race+lint+docs-lint green. Rebalance algorithm + 5s cooldown + `ShardManager` (ring+store+membership-watch composition) = Task 6; FailoverManager = Task 8; sibling StateStore/ConfigStore later. Tasks 6-18 NOT started.)_
6. **Rebalance algorithm** — minimal migration on member join/leave; cooldown 5s.
   _(landed: `internal/cluster/shardmanager.go` — **no new dep**; first task composing components (HashRing T4 + ShardStore T5 + MembershipManager T2). `ShardManager` registers as a `MembershipObserver` (via a small `membershipSource` interface for decoupling/testability): eligible members (HEALTHY/DEGRADED; LEAVING/UNHEALTHY excluded — T7 drives those edges) → `ring.Add`/`Remove` on every node. **`Owner`** = sticky persisted assignment else the deterministic ring (all nodes agree). **`Rebalance`** reconciles `ShardStore.List()` to the ring, persisting only changed agents via `AssignIf(storedVersion)` CAS (minimal by Task 4's deterministic-rebuild property; `ErrVersionConflict` → log+skip, next cycle). Membership-driven rebalances are **debounced with a ≥`rebalance_cooldown` (default 5s) spacing** (rapid flaps coalesce); manual `Rebalance` is immediate. **`AssignAgent`** durably assigns a new agent (create-only CAS). **Leader-only seam:** `LeaderCheck func() bool` (nil⇒always) — ring stays synced on all nodes (so `Owner` reads are correct everywhere) but only a leader persists writes (§4.15 "agent rebalance is a leader-only SingletonTask"); wiring `LeaderElector.IsLeader` here is **Task 9**, not this task. `RebalanceObserver` emits the `[]ShardMove` for FailoverManager (T8)/agents. New `config.ClusterShardConfig.rebalance_cooldown` (default 5s) + `ErrNoEligibleMembers`. Single-use lifecycle. `internal/cluster` 85.1% cov (>80% gate), `make lint`/`docs-lint` clean, **full `go test ./... -race` green** (cluster test timeouts/scale hardened against full-suite `-race` CPU contention; the pre-existing unrelated `internal/statemgmt/stdlib/service` race is ROADMAP-logged + intermittent — not in this diff). FailoverManager batch reassignment (T8), the SingletonTaskManager leader wiring (T9), HealthMonitor health edges (T7) NOT started.)_
7. **`HealthMonitor`** — pluggable checkers (heartbeat, etcd, DB, NATS); consecutive-failure tracking; observers.
   _(landed: `internal/cluster/health.go` — **no new dep**. `HealthChecker` iface + `PingChecker` adapter (DB/NATS wired by boot as ping funcs — keeps `internal/cluster` dep-clean) + built-in `NewEtcdChecker` (bounded Get; reaching etcd ⇒ quorum) + `NewHeartbeatChecker` (etcd keepalive-failure counter). Per-checker consecutive-failure threshold (default 3, §4.15) + P50/P99 via a fixed `latencyRing` (nearest-rank). **Quorum/partition detection**: `LoadMembers` error OR a failing critical etcd checker ⇒ `QuorumMinority` (enforcement — minority write-block — is FencingManager/T11, this only detects+signals). Aggregate → `MemberStatus`, applied through the **Task 2 SM via valid edges only**: a stepwise walk (`HEALTHY→DEGRADED→UNHEALTHY`; recover direct) so the invalid `HEALTHY→UNHEALTHY` jump is never attempted — **this is the consumer that finally drives `MembershipManager.SetStatus`**, closing the loop with Task 6's `eligible()` predicate. `HealthObserver` fan-out; single-use lifecycle. New `config.ClusterHealthConfig` (`check_interval` 5s / `failure_threshold` 3 / `latency_window` 100). Tests use a SM-enforcing `fakeSink` (asserts zero invalid transitions + exact escalation path) plus a real-MembershipManager-on-embedded-etcd integration test (member record walks HEALTHY→DEGRADED→UNHEALTHY→HEALTHY in etcd). `internal/cluster` 85.7% cov (>80% gate); `make lint`/`docs-lint` clean; **single full `go test ./... -race` green** (cluster test lease-TTL/scale hardened against `-race` contention; one assertion relaxed to the deterministic ring-consistency invariant since exact A/B hash balance at small n is variance, already covered at scale in hashring_test; unrelated pre-existing `internal/statemgmt/stdlib/service` race is ROADMAP-logged). FencingManager (T11), FailoverManager (T8), the SingletonTaskManager leader wiring (T9), real DB/NATS checker wiring (boot) NOT started.)_
8. **`FailoverManager`** — detection → reassignment with idempotency keys; cooldown.
   _(landed: `internal/cluster/failover.go` — **no new dep**. `FailoverState` SM (`IDLE→DETECTING→INITIATED→IN_PROGRESS→COMPLETED`, terminal `FAILED`/`ROLLED_BACK`). Detection = `MembershipObserver` (`MemberLeft` or `MemberUpdated`+`UNHEALTHY` marks a member failed) **correlated** with `RebalanceObserver` moves whose `From` ∈ the failed set (a move From a healthy member is join-driven rebalancing — correctly ignored). Episode worker (worker-ctx; observer callbacks only enqueue): per-member signal **dedup via `armed`** (the detect+rebalance signal pair for one failure coalesces into a single episode; cleared at episode end so a genuine later failure re-arms) + a `failoverSettle` grace so the near-simultaneous rebalance moves land before snapshot. Pluggable `AgentReassigner`/`JobReassigner` (+`AgentReassignerFunc`; boot wires the real controlplane/agent — keeps internal/cluster dep-clean): agents reassigned in batches of `AgentBatch` (100), jobs in `JobBatch` (50) each with a **deterministic idempotency key** `failover/<member>/<episodeID>/<batchIdx>` so a retried/duplicate reassignment never double-executes; reassigner error → `FAILED` (+ optional `Rollback` hook → `ROLLED_BACK`). **10s failover cooldown** (config, distinct from the 5s shard rebalance). **Leader-gated** `LeaderCheck` seam (nil⇒always; non-leader observes but doesn't orchestrate — wiring `LeaderElector.IsLeader` is Task 9). `FailoverObserver` fan-out; single-use lifecycle. New `config.ClusterFailoverConfig` (cooldown 10s / agent_batch 100 / job_batch 50). Tests are **fully deterministic** (fakes + direct observer-method calls, no embedded etcd) — also rewrote the Task 6 `RebalanceCooldownSpacing` to inject topology via `ShardManager.OnMembershipChange` directly (removes etcd-watch latency that flaked it under full-suite `-race`). `internal/cluster` >80% cov; `make lint`/`docs-lint` clean; single full `go test ./... -race` green (unrelated pre-existing `internal/statemgmt/stdlib/service` race ROADMAP-logged). Real CommandDispatcher/agent-reconnect wiring (boot), SingletonTaskManager leader wiring (T9), RecoveryManager (T10), FencingManager (T11) NOT started.)_
9. **`SingletonTaskManager`** — leader-only tasks; transition on leadership change.
   _(landed: `internal/cluster/singleton.go` — **no new dep**. `SingletonTask` iface + `StartStopTask(name,start,stop)` adapter (wraps any Start/Stop component e.g. a RetentionEnforcer) + `LoopTask(name,interval,fn)` (the cleanup/metric-aggregation pattern; worker-ctx). `SingletonTaskManager` observes a `leadershipSource` (`*LeaderElector` — `AddObserver(LeadershipObserver)`/`RemoveObserver`/`IsLeader`), `Register`s tasks before Start, and on `OnLeadershipChange` Starts all tasks when `ev.Self` (LeaderElected) / Stops them when not — **idempotent + `txMu`-serialised** so repeated Elected or a gain/lose race never double-starts; a task's Start error is logged and siblings still start; Start does an initial leadership sync. **`LeaderCheck() func() bool`** is the single canonical leader accessor boot wiring will pass into the ShardManager/FailoverManager/RetentionEnforcer seams. Single-use lifecycle. **Scoping/layering decision (honest):** the actual `cmd/kscore-server` boot wiring of `LeaderCheck()`/`le.IsLeader` into ShardManager (T6) + FailoverManager (T8) + the `internal/audit`/`internal/events` `RetentionEnforcer` `WithRetentionLeaderCheck`/`AlwaysLeader` seams is **NOT done here** — it depends on clustering being constructed at server boot (deferred since T1). Logged as a `gate-v1.0` ROADMAP entry ("Cluster leader-check boot wiring (kscore-server)", mirrored in `tools/trackerctl/config/release-order.yaml`); lands with the server-lifecycle/boot integration (T14 or a dedicated boot task). Reactor-coordinator + scheduled-jobs singleton tasks are **v1.1** (epic scope) — STM hosts them later. Tests deterministic (fake leadershipSource/tasks + direct `OnLeadershipChange`) + one real-LeaderElector-on-embedded-etcd integration. `internal/cluster` >80% cov; `make lint`/`docs-lint` clean; single full `go test ./... -race` green (unrelated pre-existing `internal/statemgmt/stdlib/service` race ROADMAP-logged). RecoveryManager (T10), FencingManager (T11) NOT started.)_
10. **`RecoveryManager`** with 7 phases.
   _(landed: `internal/cluster/recovery.go` — **no new dep**. One-shot `Recover(ctx)` orchestration (not a daemon) driving the §4.15 machine `STARTING→CONNECTING→SYNCING→VERIFYING→REJOINING→RECLAIMING→COMPLETED` with terminal `FAILED` (wraps the failing phase). CONNECTING = bounded etcd `Get` probe with `connect_retries`/`connect_timeout` backoff. SYNCING loads the shard map (`ShardStore.List` — the operative input, no registration needed) + best-effort member list (`MembershipManager.LoadMembers` is gated on registration pre-REJOIN, so **`ErrNotRegistered` is tolerated**, not a failure — honest handling of the API reality vs the spec ordering). VERIFYING structurally validates assignments. REJOINING = `Membership.Register` (fresh ephemeral lease under the **stable Task-2 member ID** — the contract that makes reclaim correct). RECLAIMING re-lists the (post-rejoin) shard map, filters `MemberID == Self().ID`, hands them to a pluggable `AgentReclaimer` (+`AgentReclaimerFunc`; boot wires the real agent-runtime — keeps internal/cluster dep-clean). Composed via narrow interfaces (`recoveryEtcd`/`recoveryMembership`/`recoveryShards`) for deterministic fakes. Single-use (second `Recover` → `ErrAlreadyStarted`); `RecoveryObserver` per-phase. New `config.ClusterRecoveryConfig` (connect_timeout 5s / connect_retries 3). Tests deterministic (fakes, per-phase failure injection, exact phase sequence, RECLAIMING member-filter) + one embedded-etcd integration proving stable-ID reclaim (pre-seed shard map, fresh MM same MemberID → reclaims exactly its agents). `internal/cluster` 88.1% cov (>80% gate); `make lint`/`docs-lint` clean; single full `go test ./... -race` green (unrelated pre-existing `internal/statemgmt/stdlib/service` race ROADMAP-logged). StateStore/ConfigStore (full state stores), real agent-reclaim boot wiring, the `<15s` SLO (T17/18 E2E), FencingManager (T11) NOT started.)_
11. **`FencingManager`** — lease + epoch fencing; modes.
   _(landed: `internal/cluster/fencing.go` — **no new dep**; the split-brain *enforcement* (HealthMonitor/T7 only *detects* `QuorumMinority`). `FenceMode` STRICT/READ_ONLY/GRACEFUL (default **READ_ONLY** = §4.15 acceptance "minority blocks writes, reads continue"). Composes (narrow ifaces, dep-clean) `*HealthMonitor` (HealthObserver — quorum-loss ⇒ `fencedQuorum`; this realises **lease fencing**: no quorum ⇒ membership lease can't renew ⇒ fenced), `*LeaderElector` (LeadershipObserver — on win, **bumps an etcd epoch** at `<prefix>/fence/epoch` via `Txn` CAS), `*EtcdClient` (Get/Txn/**Watch** the epoch). **Epoch fencing**: watch the epoch key; if it advances past the value this node set while it still believes it leads (a deposed/partitioned old leader) → `fencedEpoch` self-fence; cleared when LeaderElector reports loss. `Guard(OpType) (release,error)` enforces per mode (in-flight counter); `Drain(ctx)` (GRACEFUL); `CurrentEpoch()`/`ValidEpoch(e)` (op snapshots epoch, re-checks before commit — stale ⇒ abort); `FenceObserver`. New `config.ClusterFencingConfig.mode` (validated enum, default read_only). Single-use lifecycle. **Honest scoping (like T9):** wiring `Guard`/`ValidEpoch` around the actual server write paths is a `cmd/kscore-server` boot/middleware concern (no cluster→boot wiring since T1) — logged as a `gate-v1.0` ROADMAP entry "Fencing guard wired around server write paths" (mirrored in `tools/trackerctl/config/release-order.yaml`); lands with the server-boot integration (T14 / dedicated boot task). Tests deterministic (fakes incl. a `clientv3.Txn`/Watch fake — modes, quorum fence/unfence, GRACEFUL Drain, epoch staleness via watch, deposed-leader self-fence) + one embedded-etcd integration (two managers, real `Txn` CAS epoch bump + Watch → the stale one self-fences). `internal/cluster` 88.4% cov (>80% gate); `make lint`/`docs-lint` clean; single full `go test ./... -race` green (unrelated pre-existing `internal/statemgmt/stdlib/service` race ROADMAP-logged). CoordinationService (T12-13), the `<1s` partition SLO + split-brain E2E (T17/18), per-write lease-ID refinement NOT here.)_
12. **`CoordinationServer`** (mTLS gRPC) — full RPC set.
   _(landed: `internal/controlplane/grpc_coordination_server.go` + realigned `api/proto/.../coordination.proto` — **no new dep**. Proto realigned **purely ADDITIVELY** (new fields/messages `MemberInfo`/`ShardAssignmentInfo`; kept the deliberate Epic-03 `LookupLeader`/`NodeHeartbeat` names for type-name uniqueness) so it stays wire-compatible — `buf generate` regenerated the 2 `coordination*.pb.go`; `make proto-breaking` is clean (additive) and is anyway not in the gate set. `CoordinationGRPCServer` (embeds `UnimplementedCoordinationServiceServer`, sibling of grpc_policy/events) implements all 6 RPCs: `ClusterHealth`/`LookupLeader`/`NATSStatus`/`RecoveryCoordinate`/`NodeHeartbeat`/`PropagateState`, delegating to independently-nilable injected providers (`coordHealth`/`coordLeader`/`coordMembers`/`coordShards`/`coordNATS` + a `Propagate` hook) — nil ⇒ `codes.Unavailable`, **except** NATSStatus (down is a valid answer ⇒ "unknown") and RecoveryCoordinate (best-effort partial snapshot — a recovering peer wants whatever's available). `internal/controlplane`→`internal/cluster` is a clean one-way import. **mTLS-only enforcement (the acceptance criterion):** `requireMTLS(ctx)` at every RPC head — `peer` → `credentials.TLSInfo`, zero `VerifiedChains` ⇒ `codes.Unauthenticated`. Tests: deterministic direct-call + crafted `peer.Context` (verified-chain pass / no-peer / no-chain fail across all 6 RPCs) + delegation/translation + a bufconn **insecure-dial e2e proving non-mTLS rejection end-to-end**. No config change (mTLS always-on by contract). **Honest scoping (recurring pattern):** registering this on a dedicated mTLS server↔server listener is `cmd/kscore-server` boot wiring — deferred (no cluster→boot wiring since T1), logged as `gate-v1.0` ROADMAP "Cluster gRPC services boot registration (ClusterService/CoordinationService + mTLS listener)" (+ trackerctl mirror; forward-covers T15 ClusterService registration). `internal/controlplane` 76.7% cov (>70% critical gate; coordination server ~100%); `make lint`/`docs-lint` clean; single full `go test ./... -race` green (unrelated pre-existing `internal/statemgmt/stdlib/service` race ROADMAP-logged). CoordinationClient (T13), ClusterService (T15), NATS-down runtime orchestration + E2E (T17) NOT started.)_
13. **`CoordinationClient`** with connection pool + heartbeat tracking + retry with exp backoff.
   _(landed: `internal/controlplane/coordination_client.go` — **no new dep**; peer side of the Task 12 mTLS channel, alongside the server (gRPC-layer infra; stays out of pure `internal/cluster`). Per-peer **connection pool** (`map[memberID]*peerConn`, lazy `grpc.NewClient`; `AddPeer`/`RemovePeer`/`SetPeers` reconcile). All 6 RPC wrappers call the generated `v1.CoordinationServiceClient` through **`withRetry`**: per-attempt timeout + capped exponential backoff, retrying only transient codes (`Unavailable`/`DeadlineExceeded`), never `Unauthenticated`/etc.; honours ctx. **Heartbeat liveness**: `Start` ticker NodeHeartbeat's every peer; success→`reachable`/`lastSeen`/fails=0, ≥`FailureThreshold` consecutive fails→`reachable=false`; `PeerLiveness`/`ReachablePeers` + a `PeerObserver` reachability-flip hook. mTLS dialing injected as `DialOptions` (boot: identity-provider mTLS creds; tests: bufconn+insecure). Single-use lifecycle (idempotent Stop closes all conns + empties the pool). New `config.ClusterCoordinationConfig` (heartbeat_interval 5s/heartbeat_timeout 2s/failure_threshold 3/retry_max 4/retry_base_delay 100ms/retry_max_delay 2s; validated incl. base≤max). Tests deterministic: a controllable fake `CoordinationServiceServer` over bufconn — pool add/remove/set + unknown-peer NotFound, per-RPC delegation + FromNodeId propagation, retry-transient-then-success / no-retry-on-permanent / exhausted / ctx-cancel-mid-backoff, heartbeat reachable→unreachable→recover + observer flips, lifecycle. **Honest scoping (recurring):** driving `AddPeer`/`RemovePeer` from MembershipManager + real mTLS creds + the NATS-down switchover is boot/integration — covered by the existing `gate-v1.0` ROADMAP "Cluster gRPC services boot registration …" entry (references extended to it; no new entry — avoids proliferation). `internal/controlplane` 77.5% cov (>70% critical gate); `make lint`/`docs-lint` clean; single full `go test ./... -race` green (unrelated pre-existing `internal/statemgmt/stdlib/service` race ROADMAP-logged). ClusterService (T15), NATS-failure E2E (T17) NOT started.)_
14. **Graceful shutdown** sequence (5 phases) wired into Epic 04 server lifecycle.
   _(landed: `internal/cluster/shutdown.go` — **no new dep**. **Scope (explicit, approved):** the self-contained `GracefulShutdown` *orchestrator* + the 5-phase sequence + hook points — NOT the full cmd/kscore-server boot integration (that's the large deferred umbrella; doing it here would violate one-task-one-PR). One-shot `Shutdown(ctx)` (single-use → `ErrAlreadyStarted`): `RUNNING→INITIATED→DRAINING→TRANSFERRING→DEREGISTERING→COMPLETED`, terminal `FAILED` only when Deregister errors. DRAINING runs the `StopAccepting` hook then `Membership.SetStatus(LEAVING)` — the ShardManager membership-observer then rebalances this node's agents onto peers *before* exit (delivers "no agent disconnections seen by clients"). TRANSFERRING calls `Leadership.TransferLeadership` only if `IsLeader()`. DEREGISTERING does `Drainer.Drain` (FencingManager GRACEFUL) best-effort within `cluster.shutdown.timeout` (§4.15 **30s**; a slow/blocked drain is logged and never prevents key removal) then `Membership.Deregister` (revoke lease → member key gone); Deregister error → `FAILED`. Narrow nilable collaborator ifaces (`shutdownMembership` required; `shutdownLeadership`/`shutdownDrainer`/`StopAccepting` optional) → deterministic fakes; `ShutdownObserver`. New `config.ClusterShutdownConfig.timeout` (30s, validated). Tests: happy-leader exact phase order + call order (setstatus→deregister), non-leader skips transfer, nil collaborators, drain-timeout-still-deregisters, Deregister-error→FAILED, SetStatus-error best-effort, single-use, observer. **Honest scoping:** the SIGTERM→`Shutdown` hookup + real `StopAccepting` (conn-manager/listener drain) + boot construction folded into the existing `gate-v1.0` ROADMAP "Cluster gRPC services boot registration …" entry (extended; no new entry → `release-order.yaml` unchanged). `internal/cluster` >80% cov; `make lint`/`docs-lint` clean; single full `go test ./... -race` green (unrelated pre-existing `internal/statemgmt/stdlib/service` race ROADMAP-logged). ClusterService (T15), HA E2E "no disconnections"/zero-downtime SLO (T17) NOT started.)_
15. **`ClusterService` gRPC + REST handlers**.
   _(landed: `internal/controlplane/grpc_cluster_server.go` + `pkg/api/cluster/handler.go` (rewritten from the Epic-03 501-stub) + `internal/cluster/backup.go` — **no new dep** (`github.com/google/uuid` already in the module). `cluster.proto` realigned **purely ADDITIVELY** (`CreateBackupResponse.snapshot bytes`, `RestoreBackupRequest.{snapshot bytes,force bool}`; existing Epic-03 names/numbers untouched) → `buf generate` regenerated; `make proto-breaking` clean (additive) and not in the gate set anyway. **`ClusterGRPCServer`** (embeds `UnimplementedClusterServiceServer`, sibling of grpc_coordination/policy/events) implements all 12 RPCs over independently-nilable injected providers (`clusterLeader`/`clusterMembers`/`clusterRebalancer`/`clusterShardStore`/`clusterLeaderWatch` + reused `coordHealth` + an `Evictor` hook) — nil ⇒ `codes.Unavailable`; `internal/controlplane`→`internal/cluster` stays a clean one-way import. `GetClusterStatus`/`ListMembers`(+status filter)/`GetMember`(`ErrMemberNotFound`→`NotFound`)/`GetLeader`/`TransferLeader`(non-leader→`FailedPrecondition`)/`Rebalance`; `AddMember`→`Unimplemented` **by contract** (members self-register on start — no add in the etcd membership model); `RemoveMember` nil-Evictor→`Unimplemented`; `Rebalance{DryRun}`→`Unimplemented` (planning is v1.1); `WatchMembership`/`WatchLeadership` streams (buffered observer, drop-if-full, defer-RemoveObserver). **`CreateBackup`** is leader-gated (`FailedPrecondition` off-leader — §4.15 "leader-initiated backup ensures ordering") and returns a self-describing snapshot blob; **`RestoreBackup`** decodes+validates (`ErrInvalidSnapshot`→`InvalidArgument`), `DryRun` validates only. **`internal/cluster/backup.go`**: versioned envelope `magic[16]||formatV1||JSON` (`ClusterSnapshot{Meta,Members,Shards,ConfigJSON}`), `MarshalSnapshot`/`UnmarshalSnapshot` (bad magic/version/body → `ErrInvalidSnapshot`), `BuildSnapshot`, `RestoreShards(force?Assign-overwrite-all : AssignIf(create-only,skip ErrVersionConflict))` — pure, etcd-free, deterministic-fake-tested. **REST** `pkg/api/cluster`: `ClusterProviders{Status,Leader,Members,Rebalance,Backup}` (each nilable → 503 `unavailable()`), `GET status|leader|members|members/{id}`, `POST leader/transfer`(non-leader→409)`|members`(→501 by contract)`|rebalance|backup`(octet-stream)`|restore`(`ErrInvalidSnapshot`→400), `DELETE members/{id}`; lowercase-status DTO per the events/policy REST convention; watch streams stay gRPC-only (events-REST precedent). Tests deterministic: bufconn gRPC rig (status/members/leader, add/remove via Evictor, transfer+rebalance guards, backup↔restore roundtrip, nil-providers→Unavailable, both streams) + httptest REST (nil→503, status/members/leader, transfer leader-guard, remove+rebalance, backup↔restore roundtrip + garbage→400) + `internal/cluster` backup unit (roundtrip, envelope validation, RestoreShards force/no-force). `internal/controlplane`/`internal/cluster`/`pkg/api/cluster` >gate cov; `make lint`/`docs-lint` clean; single full `go test ./... -race` green (unrelated pre-existing `internal/statemgmt/stdlib/service` race ROADMAP-logged). **Honest scoping (recurring pattern):** wiring real providers + the operator-surface gRPC registration + the `Evictor` (MembershipManager) at `cmd/kscore-server` boot is folded into the existing `gate-v1.0` ROADMAP "Cluster gRPC services boot registration …" entry (extended in place; no new entry → `release-order.yaml` unchanged) — until then REST routes 503 and the gRPC server is unit/bufconn-tested only. `kscore-cluster`/`kscore-cluster-backup` CLIs (T16), HA E2E (T17), SLO verification (T18) NOT started.)_
16. **`kscore-cluster` CLI** + **`kscore-cluster-backup` CLI**.
   _(landed: `cmd/kscore-cluster/` + `cmd/kscore-cluster-backup/` (thin `main` wrappers, the kscore-policy precedent) over a shared **`internal/cli/cluster`** package — **no new dep** (`cobra` + `grpc` already in module). `NewClusterCommand`: `status` (GetClusterStatus + ListMembers — member list/leader/health/quorum, the acceptance line), `members [id] [--status]`, `leader`, `add` (**honest contract passthrough** — AddMember is `Unimplemented` server-side since members self-register; the CLI surfaces that verbatim rather than hiding the documented verb), `remove <id> [--force]`, `transfer-leader [--target]`, `rebalance [--dry-run]`, `backup`/`restore`. `NewBackupCommand` (DR tool): shared remote `backup [--output] [--description]` / `restore --input [--force] [--dry-run]` over `CreateBackup`/`RestoreBackup`, plus **local server-free** `list [dir]` + `verify --input` (scan/validate `*.kscore` files via `internal/cluster.UnmarshalSnapshot` — the policy eval/validate CI-friendly precedent; `verify` non-zero exit on `ErrInvalidSnapshot`). Reuses the exact kscore-policy conventions: `--server` (default `localhost:9090`), `--api-key`/`KSCORE_API_KEY` → `authorization: Bearer`, `-o table|json` (protojson for proto, indented JSON for local), insecure dial (CLI mTLS = shared v0.x ROADMAP carry), `Deps{Dial}` injection for bufconn tests. `internal/cli/cluster`→`internal/cluster` is a clean import (consistent with `pkg/api/cluster`). Tests deterministic: a self-contained in-process `fakeServer` (no controlplane internals) over bufconn drives every remote subcommand (status/members+filter/not-found, leader/transfer/remove/rebalance, add→Unimplemented surfaced, backup↔restore roundtrip + stdout, JSON + invalid-`-o`) + local list/verify over `t.TempDir()` (valid + garbage) + both `--help` smoke tests. `internal/cli/cluster` **78.9% cov** (>40% CLI gate); `make lint`/`docs-lint` clean; single full `go test ./... -race` green (unrelated pre-existing `internal/statemgmt/stdlib/service` race + `internal/events` JetStream load flake ROADMAP-logged, pass isolated). **Scoping (stated):** `kscore-cluster-backup schedule` (backup automation — epic-tagged future) + a `kscore-cluster watch` (live membership/leadership tail over the existing task-15 streams; not in the FEATURES/acceptance CLI list) deferred under a new `v1.x` ROADMAP entry "kscore-cluster-backup schedule + kscore-cluster watch subcommands" (mirrored in `release-order.yaml`). Live use against a real server stays gated by the existing `gate-v1.0` "Cluster gRPC services boot registration …" entry (ClusterService not yet registered at boot); the CLI itself is fully bufconn-tested. HA E2E (T17), SLO verification (T18) NOT started.)_
17. **HA E2E tests** — `test/e2e/ha_*_test.go`:
    - **NATSFailure**: stop NATS node, verify delivery via CoordinationService, restart, verify resumption.
    - **EtcdFailure**: stop etcd node, verify quorum maintained, restart.
    - **DatabaseFailover**: stop/restart Postgres; verify reconnect + zero data loss.
    - **NetworkPartition**: iptables partition; majority operates; minority blocks writes within 1s.
    - **SplitBrain**: symmetric partition; quorum enforcement; automatic healing.
   _(landed: `test/e2e/ha/` — new `ha` package, all `_test.go`, `//go:build integration` (excluded from `make test`; CI runs it on every PR via the existing `make test-integration` job in both `.woodpecker/ci.yml` + `.github/workflows/ci.yml`). **No new dep** (stdlib `crypto/*` for in-test mTLS; `lib/pq` already a dep). **Honest scope (the recurring boot-wiring pattern):** a literal multi-process 3×`kscore-server` / real-iptables form is blocked by the `gate-v1.0` boot-registration entries (ClusterService/CoordinationService not registered at boot, `FencingManager.Guard` not wired around write handlers, `SingletonTaskManager` not constructed at boot). This suite is the **harness those entries graduate against** — it proves the HA *mechanisms* in-process now against the **real** `internal/cluster` stack + **real** `CoordinationGRPCServer` over a **real mTLS TCP listener** + **real embedded etcd** + (for DB) **real Postgres**; the full server-integrated form lands when boot wiring does. The "3-node cluster" is the §4.15 model: N `MembershipManager`+`LeaderElector`+`ShardManager`+`FailoverManager` on one shared embedded `EtcdClient` (embedded etcd ≤3; Keystone members keyed by ID). `harness_test.go` + 6 scenario files: **ClusterFormsAndElectsLeader** (3-node forms, single-leader invariant held continuously — never split), **AddMemberMinimalRebalance** (consistent-hash: minority moves, all onto the newcomer, survivors preserved — isolated real ShardStore/etcd/HashRing over a controllable `fakeMembers`, the proven internal `shardmanager_test` composition; `agent-%d`/large-N sampling since short ids FNV-cluster), **LeaderKillFailover** (victim leaves → survivor rebalances → `FailoverManager` episode reassigns its agents exactly once, no double-execution), **NATSFailureCoordinationChannel** (NATS down → health/leader/recovery still answerable over the mTLS CoordinationService; restored → resumes) + **CoordinationRejectsNonMTLS** (the acceptance line, real TLS listener, insecure dial rejected), **MinorityPartitionBlocksWritesWithin1s** + **SplitBrainNoDoubleLeader** + **EtcdFailureFailsSafe** (real `FencingManager` + controllable quorum seam — HealthMonitor *detection* is unit-tested separately; writes fenced within the 1s correctness budget, reads continue under `FenceReadOnly`, auto-heal on quorum return, no double-leader, etcd-down fails loudly), **MemberRestartReclaimsShards** (stable-ID recovery: isolated real ShardStore seeded, fresh `MembershipManager`+`RecoveryManager` under the same ID reclaims exactly its own agents), **DatabaseFailoverReconnectZeroLoss** (`KSCORE_TEST_POSTGRES_DSN`-gated — skips locally, runs in CI's integration job against the sidecar Postgres: forcibly terminate the store's backends, assert transparent reconnect + the pre-failure write survived). `make lint` (default + `--build-tags=integration`)/`docs-lint` clean; `go test -tags=integration -race` green + stable across repeats; default `go test ./... -race` unaffected (build-tag-excluded; the unrelated pre-existing `internal/statemgmt/stdlib/service` `-race` flake is the existing ROADMAP entry, not in this diff). **Coverage:** the `internal/cluster` >80% gate is already met by unit tests and measured by `make test-coverage` (no `-tags=integration`) — this suite is additive behavioural verification, not coverage-counted. Functional correctness only; the tight SLO *numbers* (<3s leader / <10s failover / <15s recovery) are Task 18. The literal multi-process / iptables-partition form deferred to a new `gate-v1.0` ROADMAP entry (mirrored in `release-order.yaml`); the 3 existing boot-wiring entries extended to reference this harness. Task 18 NOT started.)_
18. **Performance verification** — every SLO metric checked in CI; failures block PR merge.
   _(landed: `test/e2e/ha/slo_test.go` (`//go:build slo`) + `make slo` + a new `slo` CI job in **both** `.woodpecker/ci.yml` and `.github/workflows/ci.yml` (no Postgres sidecar — SLOs are etcd/in-process; runs on every `main` PR → failures block merge). **No new dep.** Reuses the task-17 harness: the shared helpers moved to `harness_test.go` (`//go:build integration || slo`), the task-17 functional-only helpers split into `harness_integration_test.go` (`//go:build integration`) so the `slo` build has zero unused symbols (lint clean under default + `--build-tags=slo` + `--build-tags=integration`). **`make slo` runs WITHOUT `-race`** — race instrumentation inflates wall-clock 2–10×, which would make the asserted numbers meaningless; the in-`-race` functional smoke stays in the task-17 `make test-integration` suite (your "both" decision). Each SLO sub-test measures the real interval, **always logs the measured value** (a regression that still squeaks under the bound is visible in CI output), and asserts ≤ the §4.15 bound: **first leader <3s** + **cluster forms <10s** (`TestSLO_FirstLeaderAndClusterForms`), **failover detection <5s** + **completion <10s** + **agent reassignment <10s** + zero-duplication (`TestSLO_FailoverDetectAndComplete`), **minority blocks writes <1s** (`TestSLO_MinorityBlocksWrites` — the exact bound, taken non-`-race` so it is the true number), **recovery (restart) <15s** (`TestSLO_RecoveryRestart`). Measured locally with large headroom (leader ~6ms, failover ~210ms, fence ~0ms, recovery ~1ms) — the gate exists to catch regressions, not because the mechanisms are slow. **Honest scope (the recurring boot-wiring pattern):** these are the SLOs of the real in-process mechanisms; the server-integrated end-to-end numbers (agent reconnection latency through a running multi-process kscore-server, graceful-shutdown zero-disconnect timing) ride with the existing `gate-v1.0` "HA E2E multi-process / iptables-partition form" ROADMAP entry (extended to say so — no new heading → `release-order.yaml` unchanged). `make lint`/`docs-lint`/trackerctl-mirror clean; `make slo` + `make test-integration` green + stable; default `go test ./... -race` unaffected (only build-tagged `_test.go` + Makefile/CI/docs added — no default-tag code touched). **This closes Epic 13.**)_

## Acceptance criteria

- [x] 3-node cluster forms via embedded etcd; first leader elected in <3s. _(T17 functional + T18 SLO; in-process — the boot-gated multi-process form is the dedicated ROADMAP entry.)_
- [x] Adding a 4th member triggers minimal rebalancing (consistent hash); existing assignments mostly preserved. _(T17 `AddMemberMinimalRebalance`.)_
- [ ] Removing a member triggers reassignment in <10s; agents reconnect to new shard owner with no command loss. _(Reassignment-time SLO verified — T18 failover/agent-reassign; **agents reconnecting** needs real agents through a running server → boot-gated, tracked by the boot-wiring + multi-process ROADMAP entries.)_
- [x] Killing the leader: new leader elected in <3s; failover begins, completes in <10s; idempotency keys prevent double-execution. _(T18 SLO + T17 idempotency.)_
- [x] Network partition: minority partition blocks writes within 1s; majority continues serving. _(T18 SLO `<1s` + T17 functional.)_
- [x] Restart of failed member: rejoins cluster in <15s; reclaims its shards from shard map. _(T18 SLO + T17 stable-ID reclaim.)_
- [ ] Graceful shutdown drains in-flight commands and transfers leadership; no agent disconnections seen by clients. _(Orchestrator + 5-phase sequence complete + tested — T14; the **client-observable zero-disconnect** form needs a running server + real agents → boot-gated, same ROADMAP entries.)_
- [x] `kscore-cluster status` shows member list, leader, health, quorum. _(T16 CLI, bufconn-tested.)_
- [x] `kscore-cluster-backup backup --output /tmp/backup` produces valid snapshot; `restore --input /tmp/backup --force` restores cleanly. _(T15 backup + T16 CLI.)_
- [x] CoordinationService rejects non-mTLS callers. _(T12 + T17 real mTLS listener e2e.)_
- [x] All HA E2E tests pass on every PR; performance SLOs verified. _(T17 `integration` job + T18 `slo` job, both on every PR.)_
- [x] Coverage >80% on `internal/cluster`. _(Met by unit tests since T1; measured by `make test-coverage`.)_

**Epic 13 closure (honest):** all clustering *mechanisms* are implemented, tested, and SLO-verified in-process. Two acceptance lines remain `[ ]` because their literal *client-observable* form (agents reconnecting with no command loss; no agent disconnections seen by clients during graceful shutdown) requires the cluster constructed at `kscore-server` boot with real connected agents — explicitly tracked by the `gate-v1.0` "Cluster gRPC services boot registration", "Fencing guard wired around server write paths", "Cluster leader-check boot wiring", and "HA E2E multi-process / iptables-partition form" ROADMAP entries. Epic 13 closes here; those four entries carry the server-boot-integration to v1.0.

## Risks

- **Heartbeat-timeout < 3× interval** = leader flapping. Default 5s/30s; CI must not allow tighter.
- **Slow NATS** → false failover. CoordinationService is the safety net.
- **etcd disk full** → cluster freezes. Document monitoring; emit `kscore_etcd_disk_used_bytes` metric.
- **Connection storms after failover** — rate limit + stagger reconnect at agent side.
- **Clock skew** → unexpected lease expiry. Document NTP requirement.
- **Backup at point-in-time** may catch mid-commit state — leader-initiated backup ensures ordering.
- **etcd embedded mode** scales to ≤3 members; external etcd required for 5+. Document.
- **Split-brain via clock skew or partition** — fencing is mandatory; tests must assert no double-leader scenarios.

## References

- PROJECT-DETAILS §4.15.
