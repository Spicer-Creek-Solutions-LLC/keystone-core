# Epic 12: Audit Log + Policy Engine (Audit-Mode-Only)

**Phase**: G • **Estimate**: 2 weeks • **Depends on**: 02, 03, 11 • **Blocks**: 14 (capability policy hooks)

## Goal

Two related concerns shipped together: full audit log of all sensitive ops (v1.0) AND policy engine in **audit-mode-only** (v1.0; full enforcement v1.8). v1.0 ships the full engine + audit + reporting; the `Enforcer` is wired but always returns Allowed=true regardless of evaluation. v1.8 simply flips `policy.enforcement_enabled=true`.

## Scope (in)

### Audit log (full v1.0)

- `internal/audit/` — emitter; structured events for auth decisions, secret access, command exec, state apply, policy evaluations.
- `internal/policy/audit.go` — `Auditor` (in-memory circular buffer; configurable size).
- `AuditStore` interface + `SQLitePolicyAuditStore` (WAL; indexed on `policy_id`, `actor`, `resource_type`, `timestamp`, `severity`, `allowed`). Extends `internal/state.Store`.
- `AuditEntry{ID, Timestamp, PolicyID, PolicyName, PolicyType, ResourceType, Allowed, Duration, Violations[], EnforcementMode, User, Action, Metadata}`.
- `AuditFilter{PolicyID, Allowed, Severity, ResourceType, User, Action, StartTime, EndTime, Limit}` — pagination.
- `AuditSummary{TotalEvaluations, AllowedCount, DeniedCount, ViolationsByPolicy, ViolationsBySeverity, TimeRange}`.
- `AuditRetentionPolicy{MaxAge default 90d, MaxCount default 100k, MinSeverity, RetentionInterval default 1h}`.
- `AuditRedactionConfig{RedactMetadataKeys, RedactPatterns regex[], RedactUser bool}` — applied on export.
- Audit export: JSON, JSONL, CSV via `kscore-audit` CLI.

### Policy engine (full v1.0; enforcement gated)

- `internal/policy/` — `Engine{registry, opaEvaluator, celEvaluator, builtinEvaluator}`.
- `Policy{ID, Name, Type (OPA|CEL|Builtin), Category (Security|Compliance|Operational|Cost|Custom), Severity (Low|Medium|High|Critical), EnforcementMode (Audit|Warn|Enforce), Code, Enabled, Tags, Metadata, CreatedAt, UpdatedAt}`.
- `PolicySet`, `Bindings` (resource-type → policy/set with optional action/selector).
- **Evaluators**:
  - `OPAEvaluator` via `open-policy-agent/opa/rego`; query `data.<package>.allow`; extract violations from `violations` binding (best-effort).
  - `CELEvaluator` via `google/cel-go`; vars `input`, `resource`, `action`, `user`, `context`.
  - `BuiltinEvaluator` — hardcoded rules: `require-labels`, `require-owner`, `allowed-environments`, `allowed-actions`, `deny-privileged`, `allowed-users`, `time-window`, `no-root-execution`, `require-approval`, `max-concurrent`, `resource-quota`, `pattern-allow`, `pattern-deny`. Config via JSON in policy `Code`.
- `EvaluationInput{Resource, Action, User, Context, Timestamp}`.
- `EvaluationResult{PolicyID, PolicyName, Allowed, Violations[], Warnings[], Message, Duration, EvaluatedAt}`.
- `Violation{Rule, Message, Severity, Path, Expected, Actual, Remediation}`.
- `Engine.Evaluate(ctx, policyID, input)` / `EvaluatePolicySet(ctx, setID, input)` / `EvaluateForResource(ctx, resourceType, input)`.
- **v1.0 enforcement gate**: `Enforcer` exists but **always returns `Allowed=true` regardless of evaluation result**. Config `policy.enforcement_enabled=false` (effectively hardcoded false).
- `ComplianceReport{Period, ComplianceRate, TotalEvaluations, CompliantEvaluations, NonCompliantEvaluations, PolicyStats[], TopViolations[], ViolationsBySeverity, Trend[]}`.
- `ComplianceControl{ID, Framework (CIS|SOC2|NIST-800-53|HIPAA|PCI-DSS|GDPR|ISO-27001|Custom), Title, Severity, PolicyIDs[]}`.
- `ControlMapping` — 2-way framework↔policies lookup.

### APIs

- gRPC `PolicyService` (Epic 03 protos): `EvaluatePolicy`, `EvaluatePolicySet`, `ListPolicies`, `GetPolicy`, `CreatePolicy` *(Unimplemented in v1.0; v1.8)*, `UpdatePolicy` *(v1.8)*, `DeletePolicy` *(v1.8)*, `ListViolations`, `GetComplianceReport`, `GetAuditLog`, `ListPolicySets`, `GetPolicySet`.
- REST: `/api/v1/policies` (list, evaluate, violations, compliance, audit-log).
- v1.0 CLI subset:
  - `kscore-policy list|validate|check|show|eval|test|compliance|violations`. (`create|update|delete|activate|deactivate|remediate|monitor` are v1.8.)
  - `kscore-audit log|report|export|stats|search|analyze|timeline|watch`.

## Scope (out / non-goals)

- **Enforcement modes Enforce + Warn (active blocking)** — v1.8.
- **Enforcement actions** (Block, Warn, Audit, Remediate) — v1.8.
- **Pre/post-execution hooks** — v1.8.
- **Approval workflows for policy violations** — v1.8.
- **Full policy CRUD via API** — v1.8.
- **Policy persistence** (etcd or Postgres dynamic reload) — v1.8.
- **Continuous compliance scan scheduler** — v1.5.
- **CEL custom function library** — v1.5.
- **Anomaly detection** — v1.4.

## Design summary

See `PROJECT-DETAILS.md §4.12`.

## Tasks

1. **`Auditor` (in-memory circular buffer)** + tests.
   _(landed: new `internal/audit/` package opens Epic 12 with the foundational value types + ring-buffer auditor that later tasks layer atop. **Value types** in `entry.go`: `AuditEntry` per §4.12 + **`Severity` field added** to support task 2's SQL index (spec listed Severity in AuditFilter + the index but omitted it from AuditEntry — editorial oversight); `Violation` (per-rule denial detail with own Severity); `Severity` ordered enum `Low < Medium < High < Critical` with `String` / `IsValid` / `AtLeast` / `Parse` / `MarshalText` / `UnmarshalText` / `AllSeverities()`; `EnforcementMode` enum `Audit / Warn / Enforce` with the same enum surface (v1.0 records the value on every entry but the task-10 Enforcer stub ignores it; v1.8 honors it); `PolicyType` enum `OPA / CEL / Builtin` (empty for non-policy entries — task 4's auth/secrets/exec/state-apply hooks). `NewAuditEntry(in AuditEntryInput) (AuditEntry, error)` stamps UUIDv7 `ID` (k-sortable, matches Epic 11 `Event.ID` precedent for task 2's SQL index locality) + UTC Timestamp + defaults Severity to Low + defaults EnforcementMode to Audit; rejects empty Action + unknown PolicyType. `MustNewAuditEntry` test sibling; `IsZero` + `Validate`. **`Auditor` interface** in `auditor.go` (`Emit(ctx, AuditEntry)` — **no error return** per Epic 10/11 precedent "fire and forget; never error back to caller"; strict-fail variant remains on v1.x ROADMAP entry landed during Epic 11 task 10). `NoopAuditor` default. Sentinel `ErrInvalidAuditEntry` (validation family) + `ErrAuditBufferUnusable` (config family). **`BufferedAuditor`** in `buffered.go` — FIFO ring buffer mirroring Epic 10's `secrets.BufferedAuditor` shape: capacity > 0 required (rejected with `ErrAuditBufferUnusable`); `Emit` appends + evicts oldest on overflow; `Snapshot()` returns defensive oldest-first copy (correctly handles ring-wrap via `start = next` when `count == capacity`); mutex-guarded for concurrent Emit + Snapshot; `Len()` + `Capacity()` accessors. **`MultiAuditor`** in `multi.go` fan-out across N inner auditors in registration order; nil entries dropped at construction (dense inner slice); empty `MultiAuditor()` doesn't panic; mirrors `secrets.MultiAuditor` exactly. **Tests** (98.4% coverage): `entry_test.go` covers Severity / EnforcementMode / PolicyType enum surfaces (String / IsValid / Parse / Marshal/Unmarshal round-trip / aliases), `NewAuditEntry` defaults + required-field validation + preserves provided fields, IsZero + Validate table (7 mutation cases), JSON round-trip pinning canonical field names, **UUIDv7 k-sortability** locked by 30-entry sort test mirroring Epic 11 task 1. `buffered_test.go` covers capacity rejection (0 + negative), empty Snapshot returns nil, emit-below-capacity preserves order, **FIFO eviction at capacity** with cap+2 emit verifying oldest 2 dropped, exactly-at-capacity no-eviction, defensive-snapshot (mutating result doesn't affect future calls), **N=50 concurrent emitters + 10 concurrent snapshotters under -race**, **ring-wrap regression** (after 5 emits into a cap-3 ring, Snapshot must walk from `next` not from index 0). `multi_test.go` covers fan-out to N inner + nil-skipped + empty no-panic + all-nil collapses + registration-order preservation via `funcAuditor` shim. Project `make lint` + `make test` + `make docs-lint` + `-race` clean.)_
2. **`AuditStore` interface + `SQLitePolicyAuditStore`** + retention + redaction. Extend `internal/state.Store`.
   _(landed: dual-layer SQL audit store mirroring Epic 11's events split. **Low-level** `internal/state.AuditStore` sub-interface (`CreateAuditEntry` / `CreateAuditEntriesBatch` / `GetAuditEntry` / `ListAuditEntries` / `CountAuditEntries` / `DeleteAuditEntry` / `ApplyAuditRetention`) added to `state.Store` composite; `AuditEntryStoreRecord` is the DB-shape value (Violations as JSON bytes, severity/enforcement_mode/policy_type as canonical lowercase strings, Metadata as `map[string]string`); `AuditEntryFilter` carries PolicyID/User/ResourceType/Action/Severities-IN/Allowed-pointer/Since/Until/Cursor/Limit/Descending; `AuditRetentionPolicy` carries MaxAge/MaxCount/MinSeverity. SQLite + Postgres backends both ship — Postgres uses TIMESTAMPTZ + JSONB; SQLite uses ISO-8601 text timestamps via `tsArgRequired` (matches commands/events precedent); schema indexes on policy_id, "user" (SQL reserved → quoted), resource_type, timestamp DESC, severity, allowed; PRIMARY KEY collision surfaces as `state.ErrDuplicate`; batch is all-or-nothing (SQLite tx; Postgres single multi-row VALUES); cursor pagination keyed on UUIDv7 ID; **MinSeverity exemption**: entries at-or-above threshold are NEVER deleted by retention (compliance-driven). **Consumer-facing** `internal/audit.AuditStore` typed wrapper in `sql_store.go` translates `AuditEntry` ↔ `AuditEntryStoreRecord` (JSON marshal of Violations + canonical enum stringification); `AuditQuery` (typed `MinSeverity Severity`) → `AuditEntryFilter` via `severitiesAtLeast`; `Validate()` rejects Limit < 0 and Since > Until; `AuditPage.NextCursor` set only when page is full; `Close()` is a no-op (SQL pool owned by composite); defaults — `DefaultQueryLimit=100`, `DefaultRetentionMaxAge=90d`, `DefaultRetentionMaxCount=100_000`, `DefaultRetentionInterval=1h`, `DefaultRetentionJitter=0.1`. **`RetentionConfig` + `RetentionEnforcer` scheduler** in `retention.go` mirrors `events.RetentionEnforcer` (Start/Stop/RunOnce + atomic LastRunAt/LastRunDeleted/TotalDeleted/RunsFailed metrics + first-tick-after-interval to avoid boot storm + `LeaderCheck` seam for Epic 13 raft + gosec-G118-clean publisher-owned cancelable worker context); single policy (vs events' list), so `WithRetentionPolicy(RetentionPolicy)` is singular. **Redaction** in `redaction.go`: `RedactionConfigInput` (with `[]string` patterns) → `NewRedactionConfig` compiles regex at construction and rejects malformed; `Apply(AuditEntry) AuditEntry` deep-copies and drops metadata keys, regex-replaces metadata values + violation messages, optionally blanks `User`; default replacement `"***"`; `IsNoop()` short-circuits with nil-receiver safety. **Tests** (audit 95.7% / state 44.1% — state ratio is whole-package, audit-only ratio is well above 70%): `state/sqlite_audit_test.go` CRUD round-trip + duplicate + 7-row validation table + batch all-or-nothing + pre-tx validation guard + filter table (PolicyID/User/ResourceType/Action/Severities IN/Allowed pointer/time range) + cursor forward + descending pagination + Count ignores cursor/limit + Delete + retention MaxAge + MaxCount + **MinSeverity-exempt-from-deletion** (critical entries survive 25h-old + MaxAge=24h when MinSeverity=high); `state/postgres_audit_integration_test.go` (`//go:build integration`) mirrors the set; `audit/sql_store_test.go` end-to-end round-trip + validation + batch all-or-nothing + MinSeverity filter + default-limit + 3-page cursor pagination + RejectsInvalid + Count + Delete + ApplyRetention + Close idempotent; `audit/redaction_test.go` invalid-regex rejected + empty/nil receiver no-op + default replacement + RedactUser blanks + metadata-key drop + pattern replace (with input untouched) + custom replacement + all-redactors-compose + deep-copies-violations + timestamp preserved; `audit/retention_test.go` requires-store + defaults applied + invalid-values fallback + RunOnce happy + store-error recorded + leader-check-false skips + policy passed through + first-tick-after-interval (no boot storm) + ticks-repeatedly + errors-don't-stop-scheduler + Stop-is-idempotent + double-Start-rejected + concurrent RunOnce atomic + nextWait jitter ±20% bound + zero-jitter exact + String-format. `make lint` + `make test` (race) + `make docs-lint` clean.)_
3. **`AuditFilter` query builder** + pagination tests.
   _(landed: §4.12 `AuditSummary` aggregation + `IterateAll` pagination helper + hardened pagination tests. **No literal "builder" object** — `AuditQuery` struct from task 2 already covers every §4.12 `AuditFilter` field; the repo idiom is struct literals + `Validate()`, not OO builders. **`AuditSummary{TotalEvaluations, AllowedCount, DeniedCount, ViolationsByPolicy, ViolationsBySeverity, Range}`** with `TimeRange{Start, End}` shipped on the typed `AuditStore.Summarize(ctx, AuditQuery) (AuditSummary, error)`. Low-level `state.AuditStore.SummarizeAuditEntries(ctx, AuditEntryFilter) (AuditEntrySummaryRecord, error)` pushes GROUP BY into the DB (3 queries: totals+time-range, denied-by-policy_id, denied-by-severity). **ViolationsByPolicy + ViolationsBySeverity count DENIED entries only** — they answer "what is failing policy" not "what was evaluated". Both SQLite + Postgres backends; `COALESCE(SUM(CASE WHEN allowed [=1|]/[NOT allowed] THEN 1 ELSE 0 END), 0)` for portable boolean tallying. `Range.Start`/`Range.End` are zero on empty match; MIN/MAX on RFC3339Nano TEXT (SQLite) / TIMESTAMPTZ (Postgres) lex-sorts to time order. Cursor/Limit/Descending ignored — pagination doesn't apply to aggregations. **`IterateAll(ctx, store, query, fn, opts...) error`** in `internal/audit/iterate.go` walks every page via NextCursor; `WithIterateMaxPages(n)` option caps the loop (default `DefaultIterateMaxPages=10_000` — corrupted cursor can't infinite-loop, surfaces `ErrIterateMaxPages`); fn-returned error aborts iteration immediately; ctx cancel checked between pages; mutates a local copy of `query.Cursor` between pages. Nil store + nil fn rejected at entry. **Tests** (audit 95.5% coverage; state package overall 47.2%): `state/sqlite_audit_test.go` adds Summarize_Empty + Summarize_Mixed (7-row seed with 2/5 allowed/denied split + 2 policies + 3 severities + time-range verification) + Summarize_FilterNarrows + Summarize_IgnoresCursorAndLimit. `state/postgres_audit_integration_test.go` mirrors the three Mixed/FilterNarrows/Empty cases behind `//go:build integration`. `audit/summary_test.go` (8 tests) covers Empty + CountsByOutcome + ByPolicyOnlyCountsDenied (4 allowed for p-a verifies allowed entries don't leak into by-policy) + BySeverityTypedKeys (typed `map[Severity]int` keys with allowed-entry isolation) + TimeRange + FilterNarrows + MinSeverityNarrows + RejectsInvalidQuery + IgnoresCursorLimit. `audit/iterate_test.go` (10 tests) covers HappyPath (25-row walk via Limit=10 → 3 pages → 25 callbacks in order) + StopsOnFnError + CtxCancelBetweenPages + MaxPagesCap + EmptySetReturnsNil + SinglePageNoCursor + NilStore/NilFn rejection + DescendingWalks + WithIterateMaxPages_RejectsNonPositive. `audit/pagination_test.go` (10 tests) covers CursorPastEndReturnsEmpty + EmptySetNoCursor + LimitEqualsTotal (with explanation of full-page cursor semantics) + LimitGreaterThanTotal + DescendingFullWalk + CombinedFilterWithCursor (8 matching + 4 non-matching across PolicyID/User/Allowed/MinSeverity + 3-page walk) + StableUnderIntermediateDeletes (deleted entries from page1 don't double-skip page2) + TimeRangeFilterWithCursor + InvalidCursorHandledGracefully + DefaultLimitOnEmptyLimit + ExactlyAtDefaultLimitSetsCursor. `make lint` + `make docs-lint` clean; `internal/audit/` + `internal/state/` race-clean. **Note**: pre-existing data race in `internal/statemgmt/stdlib/service/systemd_test.go:282` (`t.Parallel()` tests mutate `systemdRunDir` global) discovered during `make test` — present at `3f490c11`, outside task 3's touched scope. Logged in ROADMAP under v0.x bucket: "`service` stdlib module — make systemdRunDir test-mutable without a package-level global"._
4. **Audit emission hooks** wired into: auth (Epic 03), secrets (Epic 10), state apply (Epic 08), command exec (Epic 07), policy evaluation (this epic). **Hard rule**: every sensitive op MUST emit an audit entry. CI test that exercises each path and verifies entry present.
   _(landed: 4 of 5 sensitive ops wired — policy eval **deferred to tasks 5+9** per the user-confirmed plan (Engine doesn't exist yet; tasks 5/9 own the surface and plant the seam there). **`audit.StoreAuditor`** in `internal/audit/store_auditor.go` wraps an `AuditStore` so it satisfies the `Auditor` interface — Emit calls Store; errors logged via slog + `FailedStores` atomic counter; never propagated (Epic 10/11 fire-and-forget precedent). Nil receiver + nil store safe. **`audit.StateApplyObserver`** in `internal/audit/state_observer.go` implements `statemgmt.RunObserver` and emits one `AuditEntry` per `Done` with `Action="state.apply"`, `ResourceType=decl.Module`, `Allowed=(Outcome != Failed)`, severity Low/High by outcome; failed decls carry the `result.Error` as a `Violation`; **Skip suppresses emission** — skipped declarations never ran, so there's no sensitive op to audit. Wraps an optional inner observer; forwards every method to it before audit emission so audit can't suppress the wire-level event stream. **`pkg/api/auth.AuthDecisionFunc`** callback on `InterceptorConfig.OnAuthDecision` fires once per Authorize result (success + failure + bypass) — callback design keeps `pkg/api/auth` free of internal-package deps. **`controlplane.TerminalCommandFunc`** on `DispatcherConfig.OnCommandTerminal` instruments all 4 terminal-state write sites in `command_dispatcher.go`: marshal-fail, publish-fail, RecordResult, sweepTimeouts; the sweep path fetches CommandRecord on the audit path so the entry has command + agent_id metadata. **`StateGRPCServer.Auditor`** field wraps the per-request `streamObserver` with `audit.NewStateApplyObserver` inside `ApplyState` — wire-level streaming unaffected. **Boot wiring** in `cmd/kscore-server/`: new `audit.go` ships `startAudit(ctx, state, log)` returning `auditRuntime{Store, StoreAuditor, Buffer, FanOut}`; `FanOut = audit.MultiAuditor(StoreAuditor, BufferedAuditor)` with `DefaultAuditBufferSize=1000` for kscore-audit's task-14 live tail. **5 emitter shims** in `cmd/kscore-server/`: `audit_store_bridge.go` (secrets → audit.AuditEntry), `auth_emitter.go` (auth decisions, severity Medium on deny), `command_emitter.go` (command exec, severity High on non-zero exit). `startSecrets` signature gained `auditStoreAuditor secrets.Auditor` so `buildAuditor` fans the SQL audit-store bridge into the secrets MultiAuditor alongside the existing events bridge. `pkg/api/server.Options` gained `CommandTerminalHook` + `StateAuditor` fields plumbed into `initStep7` (dispatcher) and `main.go` stateGRPC construction. **Wire-format invariant preserved**: `events.AuditEmitter` (Epic 11 task 10) still publishes `policy.pass`/`policy.violation` to the events bus AND the new audit-store bridge persists to SQL — dual emission per the Epic 12 task 1 architectural pillar. **CI contract test** `cmd/kscore-server/audit_emission_test.go` ships 9 tests with `AuditEmissionContract` marker comments: AuthDecision allow + deny + nil-auditor + SecretAccess + StateApply allow + StateApply skip-suppression + CommandExec terminal + CommandExec failed-severity + PolicyEval-deferred-marker. Tests exercise the actual `newAuthDecisionEmitter` / `newSecretsAuditStoreBridge` / `newCommandTerminalEmitter` / `audit.NewStateApplyObserver` functions against a recording auditor — proves the emission glue actually emits (per §4.12 "failure to log = bug"). **Acceptance criterion flipped**: "Every sensitive operation emits an `AuditEntry`" → 4/5 (policy eval deferred; task 9 will close). **Coverage**: audit 96.5%; all touched packages race-clean. `make lint` + `make docs-lint` clean. **Open carry-over**: `state.CommandRecord` has no `Principal` column, so the agent-reported terminal path (RecordResult) emits with empty `User` — logged in ROADMAP v0.x as "Persist dispatch principal on CommandRecord for audit-log User field."_
5. **`Engine` + `Registry`** in-memory; `RegisterPolicy`, `RegisterPolicySet`, `RegisterBinding`.
   _(landed: new **`internal/policy`** package — the evaluation half of §4.12, one-way dep `policy → audit` (audit never imports policy; shared `Severity` / `EnforcementMode` / `PolicyType` enums + `Violation` reused, zero translation layer). **`Policy`** struct per §4.12 + new policy-local **`Category`** enum (`security|compliance|operational|cost|custom`) with the full audit-style surface (String / IsKnown / ParseCategory / Marshal/UnmarshalText / AllCategories); empty/unknown category is an error (unlike audit.PolicyType where empty is the valid non-policy sentinel). `Policy.Validate()` (ID/Name/Type/Category/Severity/EnforcementMode/Code required + enum-valid; disabled policies still validate — Enabled gates evaluation not registration) + deep `Clone()`. **`PolicySet`** with `EnforcementOverride *audit.EnforcementMode` (pointer so "no override" is unambiguous vs the invalid Unknown zero value); `EffectiveMode(policyMode)` returns override-or-inherit; `Validate()` rejects empty/duplicate member IDs; referential integrity deferred to Registry. **`Binding`** attaches a Policy XOR PolicySet to a ResourceType with optional Action (empty = any) + Selector (subset label match, empty = any); `Validate()` enforces exactly-one-ref; `Matches(resourceType, action, labels)` short-circuits on disabled bindings; `TargetsSet()` discriminator. **`Registry`** mirrors `statemgmt.Registry` exactly (`sync.RWMutex`, write-locked Register*, read-locked Get/List hot path); deep-clone on the way in AND out so callers can't corrupt stored state through shared slice/map headers; sentinels `ErrDuplicateID` / `ErrNotFound` / `ErrDanglingReference` distinct from the shape-validation `ErrInvalidPolicy` family; **referential integrity at register time** — RegisterPolicySet rejects sets referencing unregistered policies, RegisterBinding rejects bindings referencing unregistered policy/set; `BindingsForResource` returns matched enabled bindings sorted by ID for deterministic evaluation order. No Deregister in v1.0 (CRUD is v1.8 / task 12). **`Evaluator`** interface seam (`Evaluate(ctx, *Policy, EvaluationInput) (EvaluationResult, error)`) + `EvaluationInput{Resource, Action, User, Context, Timestamp}` + `EvaluationResult{PolicyID, PolicyName, Allowed, Violations []audit.Violation, Warnings, Message, Duration, EvaluatedAt}`. **`Engine`** holds `*Registry` + an `audit.PolicyType`-keyed evaluator map; `NewEngine(reg, ...EngineOption)` rejects nil registry (`ErrEngineMisconfigured`); `WithEvaluator(pt, eval)` functional option (last-wins, ignores nil/unknown-type); `Registry()` + `Evaluator(pt)` accessors. **`Engine.Evaluate / EvaluatePolicySet / EvaluateForResource` signatures exist but return `ErrNoEvaluator`** — they resolve the policy/set/bindings (so missing-policy correctly surfaces `ErrNotFound` before the no-evaluator sentinel) then return the "dispatch lands in task 9" wrapped sentinel. Keeps the public surface stable for tasks 6-14 to compile against without doing task 9's dispatch/aggregation work. **Tests** (89.0% coverage, `-race` clean): `policy_test.go` (Category enum surface + JSON round-trip + marshal-rejects-unknown + Validate table 9 cases + Clone deep-copy), `policyset_test.go` (Validate table + EffectiveMode override/inherit/nil-receiver + Clone deep-copy incl. pointer override), `binding_test.go` (Validate table + Matches 9-case table covering action-wildcard / selector-subset / disabled-short-circuit + nil-receiver), `registry_test.go` (duplicate/invalid/dangling rejection for all 3 register paths + stores-clone + returns-clone + BindingsForResource filter+order + concurrent register/list under -race + NewRegistry isolation), `engine_test.go` (nil-registry rejection + WithEvaluator wiring/nil-ignore/unknown-ignore/last-wins + Evaluate* return ErrNoEvaluator with ErrNotFound-precedence). `make lint` + `make docs-lint` clean. No modifications to existing packages — purely additive.)_
6. **`OPAEvaluator`** wrapping `opa/rego`.
   _(landed: **first third-party dep of Epic 12** — `github.com/open-policy-agent/opa v1.16.2`, embedded `opa/v1/rego` + `opa/v1/ast` (comparison done: in-process SDK is the only credible embeddable Rego engine in Go; subprocess/sidecar/WASM rejected; OPA-in-v1.0 was an accepted pillar). `internal/policy/opa_evaluator.go` ships `OPAEvaluator` implementing `policy.Evaluator`. **Rego v1 syntax** (`opa/v1` packages default to `ast.RegoV1`; also set explicitly via `rego.SetRegoVersion`). **Fixed package `keystone.policy`** — queries the whole `data.keystone.policy` object once (one PrepareForEval + one Eval) and reads `allow` / `violations` / `warnings` keys; lets us distinguish "allow undefined" (key absent) from "allow == false" (present, false), and avoids parsing modules to discover package names. **Restricted capabilities**: `restrictedCapabilities()` starts from `ast.CapabilitiesForThisVersion()` and strips every builtin whose name matches `http.` / `net.` / `opa.runtime` + sets `AllowNet=[]string{}` (non-nil empty = NO host); a policy referencing a denied builtin **fails to compile** (surfaced as an evaluator error, not a silent no-op) — operator-supplied policies must be pure decision logic (SSRF/exfil guard). `WithOPACapabilities` escape hatch for trusted single-tenant deploys (nil arg ignored — keeps safe default). **Undefined / non-bool `allow`** → `Allowed=false` + one synthetic `audit.Violation` (`opa.no-decision` / `opa.non-bool-allow`, severity = policy's declared severity), `nil` error — a misconfigured policy is an audit signal, not an engine bug (fail-closed but visible). **Violations** best-effort: string element → `Violation{Message, Severity:policyDefault}`; object element → `rule/message/severity/path/expected/actual/remediation` mapped, severity via `audit.ParseSeverity` falling back to the policy's declared severity. **Warnings** → `[]string` (non-string elements skipped). **Compile cache**: lazy `PreparedEvalQuery` keyed `policyID + ":" + sha256(Code)` under a mutex — `Policy.Code` immutable per registration (no Deregister in v1.0) so no invalidation needed; a v1.8 re-register with changed Code naturally gets a fresh key. **Per-eval timeout** `DefaultOPAEvalTimeout=5s` (`WithOPAEvalTimeout` override) wraps only `Eval` (compile has no deadline); guards pathological policies. `input` document = `{resource,action,user,context,timestamp(RFC3339Nano)}`; caller maps referenced not copied (Evaluator contract: no mutation; Rego input is read-only). Panic-recover in `Evaluate` → wrapped `ErrInvalidPolicy`. **Tests** (`internal/policy` 90.5% coverage, `-race` clean): allow / object-violations (full field + rego-supplied severity) / string-violations (policy-fallback severity) / warnings / undefined-allow synthetic / non-bool-allow synthetic / input-binding (admin+prod allow vs guest deny) / compile-error-is-evaluator-error / **http.send-denied-by-caps** (asserts error mentions the builtin) / cache-reuse (5 evals stable) / code-change-recompiles (same ID new Code flips verdict) / eval-timeout (compute-heavy policy + 1ns) / nil-caps-override-keeps-default / interface assertion. Inlined Rego string constants instead of `testdata/*.rego` (small self-contained fixtures, no file I/O). **Dependency-override workflow**: PROJECT-DETAILS §3.2 deps table + §4.12 OPAEvaluator bullet + Gotchas, FEATURES.md, this epic line all updated in the same commit. `go mod tidy` promotes opa to a direct dep. `make lint` + `make docs-lint` clean.)_
7. **`CELEvaluator`** wrapping `cel-go` with v1.0 var schema.
   _(landed: **no new dependency** — `github.com/google/cel-go v0.28.1` already direct from Epic 11 task 5's event filter, so no comparison table. `internal/policy/cel_evaluator.go` ships `CELEvaluator` implementing `policy.Evaluator` for `audit.PolicyTypeCEL`, mirroring `OPAEvaluator`'s structure + the `internal/events/filter.go` CEL pattern. **Bool-expression contract**: a CEL policy's `Code` is a single boolean expression — `true`=allow, `false`=deny + one synthetic `audit.Violation` (`cel.denied`, severity = policy's declared severity); same fail-closed-but-visible philosophy as OPA's undefined-allow path. **AST output-type enforced**: `ast.OutputType().IsExactType(cel.BoolType)` rejected at compile (identical to `events.CompileFilter`) — a string/int expression is a compile error, not a silent mis-decision. **§4.12 5-var schema, no deviation**: `cel.NewEnv` declares `input` (composite `map(string,dyn)`) + convenience top-level `resource` (dyn map) / `action` (string) / `user` (string) / `context` (dyn map); `input.action == 'x'` and `action == 'x'` are equivalent. **`input.timestamp` is an RFC3339Nano string** for exact parity with the OPA input doc (user-confirmed: don't deviate from the 5-var spec for a 6th timestamp var; Rego has no native timestamp type so string is the consistent cross-evaluator shape) — time-window CEL policies wrap it `timestamp(input.timestamp) < timestamp('…')`. nil `Resource`/`Context` coerced to empty maps so `has(resource.x)` doesn't error on a resource-less input. **CEL error semantics differ from OPA by design**: CEL has no Rego-style "undefined" — every successful eval yields a typed value, so a CEL *runtime* error (no_such_key, type mismatch) is a genuine policy-authoring bug and is returned as an `ErrInvalidPolicy`-family **evaluator error** (per the `Evaluator` contract: "non-nil error only for evaluator-internal failures"), NOT coerced to a deny. Compile error → evaluator error; clean `false` → deny+synthetic violation. **No capability work** — cel-go is sandboxed by default (no I/O/network), unlike OPA; standard macros (`has`/`all`/`exists`/`map`/`filter`) + `timestamp`/`duration` available out of the box; no custom functions (the events `severity.at_least()` overload was events-specific). **Compile cache**: `cel.Program` keyed `policyID + ":" + sha256(Code)` under a mutex — identical lifecycle reasoning to OPAEvaluator (Code immutable per registration; v1.8 re-register with changed Code gets a fresh key). `ContextEval(ctx, …)` so caller ctx cancellation propagates. Panic-recover → wrapped `ErrInvalidPolicy`. **Tests** (`internal/policy` 90.6% coverage, `-race` clean): allow-true / deny-false-synthetic (policy-severity) / convenience-vars / input-composite / timestamp-RFC3339-string-wrapped / context-var / nil-maps-coerced-for-has / non-bool-rejected (asserts "must return bool") / compile-error / **eval-runtime-error-is-evaluator-error** (no_such_key) / cache-reuse / code-change-recompiles / standard-macros (`exists`) / interface assertion. Inlined CEL string constants (matching task 6). No spec/doc deviation → only this epic line updated (no PROJECT-DETAILS/FEATURES change, not a new dep, var schema unchanged). `make lint` + `make docs-lint` clean.)_
8. **`BuiltinEvaluator`** — implement the 13 built-in rules.
   _(landed: **no new dependency** — `github.com/gobwas/glob v0.2.3` already direct from Epic 07 targeting, used for `pattern-allow`/`pattern-deny`; no comparison table. `internal/policy/builtin_evaluator.go` ships `BuiltinEvaluator` for `audit.PolicyTypeBuiltin`, mirroring OPA/CEL structure. **One policy = one rule**: `Code` is a JSON object `{"rule":"<name>", ...params}`; multi-rule composition via `PolicySet` (task 5), no multi-rule sub-language. **All 13 rules** (user-confirmed full scope): require-labels, require-owner, allowed-environments, allowed-actions, deny-privileged, allowed-users, time-window, no-root-execution, require-approval, max-concurrent, resource-quota, pattern-allow, pattern-deny. Dispatch via a `ruleBuilders` map keyed by rule name; each builder parses+validates its config and returns a `compiledRule` closure (`func(EvaluationInput, audit.Severity) []audit.Violation`). **Two distinct error classes** (deliberate): config errors (malformed JSON, unknown rule, empty required arrays, bad glob, bad tz/time/day) are the author's fault → `ErrInvalidPolicy`-family **evaluator error** (mirrors OPA/CEL compile errors); data-shape mismatches at eval (field missing/wrong-typed — data comes from the op, not the author) **fail closed**: deny + `audit.Violation`, nil error. **Strict truthiness** (user-confirmed): `true | "true" | "yes" | "1" | 1` truthy (strings case-insensitive); everything incl. missing key falsy — so `require-approval` with a missing flag denies (fail-closed) and `deny-privileged` on the string `"false"` passes. **Field access**: top-level keys of `Resource` (default) or `Context` via optional `"source":"resource"|"context"`, with one-level `labels` nesting for require-labels/require-owner/allowed-environments. `gobwas/glob` patterns compiled at config-build time (bad glob = config error, not per-eval surprise). `time-window` parses `tz` (`time.LoadLocation`), `start`/`end` (`HH:MM`), `days` (weekday names incl. short forms) once at build; eval converts `input.Timestamp` to tz then checks weekday-set + time-of-day window (empty days = any day; missing start/end = no time constraint). `no-root-execution` denies user `root`/`0` (default `input.user`, optional `field`/`source`). Numeric rules (max-concurrent, resource-quota) accept JSON float64/int only — a string value is a data mismatch → deny. Violations carry `policy.Severity` + Path/Expected/Actual for operator triage. **Config cache**: `compiledRule` keyed `policyID + ":" + sha256(Code)` under a mutex (same lifecycle reasoning as OPA/CEL — Code immutable per registration; v1.8 re-register with changed Code gets a fresh key). Panic-recover → wrapped `ErrInvalidPolicy`. **Tests** (`internal/policy` 90.1% coverage, `-race` clean): 30-case `RuleTable` (pass + deny for every rule incl. truthiness edges + source override + non-numeric deny + glob match/no-match), `TimeWindow` (inside/wrong-day/after-hours), `TimeWindow_TZApplied` (13:00 UTC = 09:00 EDT boundary), `ConfigErrors` (10 cases: bad JSON / unknown rule / empty required / missing field / bad glob / bad tz / bad time fmt / bad day), `SourceContextVsResource`, `CacheReuseAndRecompile`, interface assertion. Inlined JSON constants (matching tasks 6-7). No spec/doc deviation → only this epic line updated (not a new dep, rule set matches §4.12 exactly). `make lint` + `make docs-lint` clean.)_
9. **`Engine.Evaluate` / `EvaluatePolicySet` / `EvaluateForResource`** + tests covering all evaluator types.
   _(landed: **no new dependency**. Filled the three task-5 `Engine` stub bodies (`ErrNoEvaluator` placeholders) with real dispatch + aggregation; the method **signatures were frozen in task 5** so tasks 12-14 compile unchanged. `internal/policy/engine.go`: `dispatch(p, input)` routes a resolved+enabled policy to `e.evaluators[p.Type]` (`ErrNoEvaluator` if none wired) and delegates; the OPA/CEL/Builtin evaluators already stamp `PolicyID`/`PolicyName`/`EvaluatedAt`/`Duration` so the Engine propagates the result/error unchanged. **`stampTime`**: sets `input.Timestamp = time.Now().UTC()` **only when zero**, once, before any fan-out — so every member of a set/resource evaluation sees the same instant (the `Evaluator` doc's time-window-consistency requirement); a caller-supplied timestamp (historical replay) is respected. **`Evaluate(policyID)`**: resolve (`ErrNotFound`) → new **`ErrPolicyDisabled`** sentinel when `Enabled=false` (the caller asked by name; disabled is surfaced distinctly, not masked as a clean allow) → dispatch. **`EvaluatePolicySet(setID)`**: resolve set; **disabled set → `(nil, nil)`**; iterate members in declared order, **silently skip disabled members** (bulk eval — a disabled member just doesn't contribute), **fail-fast** on the first member whose evaluator returns an error (wrapped with set + member ID — an evaluator error is a misconfig that must be loud and the set verdict is genuinely unknown, so no result is fabricated); a member that no longer resolves is wrapped as a registry-integrity failure. **`EvaluateForResource(resourceType, action, labels, input)`**: `registry.BindingsForResource` (already enabled+matched+sorted) → per binding, set-targeting → `EvaluatePolicySet`, policy-targeting → resolve+skip-disabled+dispatch; results flattened in deterministic binding order; **no dedup** — a policy referenced by N bindings evaluates N times (bindings are intentional attachments; caller sees which binding produced which result); **no matched bindings → `(nil, nil)`** (allow-by-default, not an error); same fail-fast. **`AllowedAll([]EvaluationResult) bool`** exported helper encodes the §4.12 "all-or-nothing AND" set verdict (empty = vacuously true) so task 10's Enforcer + the gRPC/REST handlers get the combined decision in one call. **`PolicySet.EffectiveMode` deliberately NOT applied here**: `EvaluationResult` carries no enforcement mode and the v1.0 Enforcer ignores mode, so threading the set-level override has zero observable v1.0 effect — deferred to task 10 (the Enforcer re-derives effective mode from policy+set where it matters). **Tests** (`internal/policy` 90.2% coverage, `-race` clean) — `engine_test.go` rewritten (task-5 `ErrNoEvaluator`-expecting tests replaced): construction/wiring (nil-registry, last-wins, nil/unknown ignored); Evaluate allow/deny-propagates-violations/NotFound/**DisabledIsError**/NoEvaluatorForType/**StampsWhenZero**/**RespectsProvidedTimestamp**; EvaluatePolicySet members-in-order/**skips-disabled-members**/**disabled-set-nil**/NotFound/**fail-fast-on-evaluator-error**/stamps-once-shared; EvaluateForResource no-bindings-nil/**per-binding-no-dedup**/set-targeting-flattens/deny-in-AllowedAll/fail-fast; **cross-evaluator** `Dispatch_RoutesByPolicyType` (real `NewOPAEvaluator`+`NewCELEvaluator`+`NewBuiltinEvaluator`, asserts each routes correctly incl. CEL allow vs deny) + `PolicySet_MixedEvaluators` (OPA-allow + Builtin-deny member → `AllowedAll`=false). A controllable `stubEvaluator` (allow/err/seenTimestamp) drives the dispatch-logic tests; the real evaluators prove routing. No spec/doc deviation → only this epic line updated.)_
10. **`Enforcer` stub** — receives `EvaluationResult`; v1.0 always returns `Allowed=true`; v1.8 will honor `EnforcementMode`.
   _(landed: **no new dependency**. `internal/policy/enforcer.go` ships `Enforcer` + `EnforcementDecision{Allowed, WouldDeny, Mode, Results}`. **`Enforce(ctx, mode audit.EnforcementMode, results []EvaluationResult) EnforcementDecision`** — one method covering all three Engine entrypoints (wrap a single `Evaluate` result in a 1-slice). `WouldDeny = !AllowedAll(results)` is **always** the real verdict regardless of enabled-state or mode — §4.12 explicitly requires operators to "see what *would* have been blocked", so task 11 ComplianceReport + the audit trail read `WouldDeny` even though v1.0 never blocks. **v1.0 (enforcement disabled, the default + the §4.12 "hardcoded false")**: `Allowed` is ALWAYS true regardless of `WouldDeny`/`mode` — audit-mode-only. **`NewEnforcer(...EnforcerOption)`** + **`WithEnforcementEnabled(bool)`** seam (default false) mirrors Epic 11 `RetentionEnforcer`'s leader-check seam precedent: the real mechanism with a v1.0-safe default so the v1.8 path is tested + wiring-ready, not a dead stub. `Enabled()` accessor. **v1.8 seam (enabled=true)**: the allow/deny **gate switch** is implemented now (small + testable) — `Enforce` mode blocks on a denying verdict (`Allowed = !WouldDeny`), Audit/Warn/Unknown → allow. The **side-effects** (Warn's warn-event emission, Enforce's violation-handler invocation) are **deferred** — they need infra that doesn't exist in v1.0 (a policy-violation event type + a handler registration/dispatch mechanism); logged as a **v1.x ROADMAP entry** "Policy enforcement side-effects (Warn events + Enforce violation handlers)" (mirrored in `release-order.yaml`). **`mode` is caller-supplied**, not resolved internally — exactly the task-9 deferral (`PolicySet.EffectiveMode` is computed by the caller); keeps the Enforcer free of Engine/Registry coupling and the signature stable into v1.8. No boot wiring (tasks 12-14 + the deferred task-4 policy-eval audit hook do that). **Tests** (`internal/policy` 90.4% coverage, `-race` clean): default-disabled; v1.0 always-allows (deny verdict + Enforce mode still `Allowed=true`, `WouldDeny=true`); v1.0 allowing-verdict; v1.0 empty-results vacuous-allow; v1.8-seam 5-case gate switch (audit/warn deny→allow, enforce deny→block, enforce allow→allow, unknown→allow) with `WouldDeny` always = real verdict; explicit-false-stays-disabled. Only the epic line + ROADMAP/release-order updated (spec unchanged; no PROJECT-DETAILS/FEATURES change).)_
11. **`ComplianceReport` generator** + framework mappings.
   _(landed: **no new dependency** — reads the task-2/3 `audit.AuditStore` (`Summarize`/`Query` via `IterateAll`) + the task-5 policy domain; one-way `policy → audit` dep unchanged. **`internal/policy/framework.go`**: `Framework` enum (8 §4.12 values `cis|soc2|nist-800-53|hipaa|pci-dss|gdpr|iso-27001|custom`, full audit-style surface mirroring `Category` — String/IsKnown/ParseFramework/Marshal·UnmarshalText/AllFrameworks); `ComplianceControl{ID,Framework,Title,Severity audit.Severity,PolicyIDs}` + `Validate()` (referential integrity deliberately NOT checked — controls are reporting metadata often authored before their policies, possibly in a separate Registry) + deep `Clone()`; **`ControlMapping`** in-memory 2-way registry mirroring `policy.Registry` exactly (RWMutex, deep-clone in AND out, sentinels `ErrDuplicateID`/`ErrNotFound` reused; no Deregister — v1.8 CRUD) with `RegisterControl`/`GetControl`/`ListControls`/`ControlsForPolicy` (policy→controls)/`ControlsForFramework`/`PoliciesForFramework` (framework→deduped-sorted policy set). **`internal/policy/compliance.go`**: `ComplianceReport` exactly per §4.12 (`Period audit.TimeRange, ComplianceRate, Total/Compliant/NonCompliantEvaluations, PolicyStats[], TopViolations[], ViolationsBySeverity, Trend[]`); `ReportGenerator{store, mapping}` with `NewReportGenerator` (store required → `ErrEngineMisconfigured`; mapping nilable). **`Generate(ctx, ReportQuery{Since,Until,Framework,BucketInterval,TopN})`**: one base `Summarize` powers headline counts + `ComplianceRate`=Allowed/Total (**Total==0 → 0.0**, zero Total signals "no data" not "100% compliant") + `ViolationsBySeverity` (denied-only per AuditSummary) + **`TopViolations`** (by-policy from `ViolationsByPolicy`, sorted desc by count then PolicyID asc, capped `TopN` default 10 — zero extra queries, user-confirmed by-policy granularity; by-rule is a v1.x refinement); **`PolicyStats`** per-policy pass/fail/rate via one scoped `Summarize` per in-scope policy, the policy set **bounded** by `Framework` filter (`ControlMapping.PoliciesForFramework`; nil mapping → no PolicyStats) else the period's denying policies (deterministic sorted); **`Trend`** caller-specified `BucketInterval` (zero → no Trend; final bucket clamped to Until; **bucket count capped at 366** so a tiny interval over a long Period can't fan out into thousands of `Summarize` calls). `Since` required + must precede `Until` (`ErrInvalidPolicy`); store errors wrapped + propagated. **`ResourceAuditTrail(ctx, resourceType, ReportQuery)`** walks `audit.IterateAll` over a `ResourceType`+window-scoped `AuditQuery`, oldest-first, with the existing `WithIterateMaxPages` safety cap. **Tests** (`internal/policy` 91.7% coverage, `-race` clean): `framework_test.go` — Framework surface/Parse/Marshal-round-trip+reject-unknown, ComplianceControl Validate 8-case table, ControlMapping register/dup/notfound/invalid + 2-way lookups + stores-and-returns-clones + concurrent register/list under -race; `compliance_test.go` — in-memory deterministic fake `audit.AuditStore` (keyed Summarize by PolicyID + trend bucket by Since), headline-counts+rate, zero-total-rate-0, TopN cap+order, PolicyStats unscoped-from-violations vs framework-scoped vs nil-mapping-no-stats, Trend 3-bucket+window + no-trend-when-interval-zero + 366 cap, invalid-query (missing/reversed Since), store-error-propagates, ResourceAuditTrail ordering+unknown-empty+requires-Since. No spec/doc deviation → only this epic line updated (no new dep, shapes match §4.12). `make lint` + `make docs-lint` clean.)_
12. **gRPC `PolicyService` server** with Unimplemented for v1.8-gated CRUD.
   _(landed: **no new dependency**. **Proto realign** (`api/proto/keystone/core/v1/policy.proto`) — replaced the epic-03 placeholder messages (`PolicyEngine`/`PolicyStatus`/`ComplianceStatus` enums, `PolicyDecision`) with the real task-5..11 domain: `Policy`/`PolicySet`/`Violation`/`EvaluationInput`(google.protobuf.Struct resource+context)/`EvaluationResult`/`AuditEntry`/`PolicyStat`/`ViolationCount`/`TrendPoint`/compliance response; string enums carry canonical lowercase names so DTO mapping is `enum.String()`/`Parse*`. Service gained `GetPolicy`/`GetPolicySet` + the three v1.8 CRUD RPCs (`CreatePolicy`/`UpdatePolicy`/`DeletePolicy`). `buf generate` regenerated; `buf lint` clean (pre-existing state.proto warnings unrelated). **`make proto-breaking` flags the rewrite** (deleted placeholder enums/messages) — **expected at v0.x**, same precedent + handling as Epic 11 task 6's event.proto realign; not part of the `make lint`/`make test`/`make docs-lint` gate set. `pkg/api/v1/api_test.go` smoke ref updated `PolicyDecision`→`EvaluationResult`. **`internal/controlplane/grpc_policy_server.go`**: `PolicyGRPCServer` embeds `v1.UnimplementedPolicyServiceServer` (so the un-overridden CRUD RPCs auto-return `codes.Unimplemented` — genuinely not implementable in v1.0: the Registry has no Update/Deregister by design, not lazy). Composes `*policy.Engine` + `*policy.ReportGenerator` + `audit.AuditStore` + `audit.Auditor`, each independently nilable → its RPC group returns `codes.Unavailable` (SecretsGRPCServer/grpc_events precedent). `translatePolicyError`: `ErrNotFound`→NotFound; `ErrPolicyDisabled`/`ErrNoEvaluator`→FailedPrecondition; `ErrInvalidPolicy`/`ErrInvalidAuditEntry`→InvalidArgument; `ErrEngineMisconfigured`→Internal. Offset pagination for List* (page_token = int offset, size default 50 cap 500); cursor passthrough for GetAuditLog/ListViolations (audit store's native k-sortable cursor); ListViolations forces `Allowed=false`. DTO mappers proto↔domain (string-enum, `time.Time`↔Timestamp, `audit.Violation`↔proto, `structpb`↔`map[string]any`). **Policy-eval audit emission wired** (`emitPolicyAudit`): `EvaluatePolicy`/`EvaluatePolicySet` emit one `audit.AuditEntry` per evaluation (`action="policy.evaluate"`, severity = max violation severity, policy type from registry) via the configured `audit.Auditor` — completes the §4.12 "every sensitive op MUST emit" rule for the **5th** sensitive op (task 4 deferred this to the engine landing in tasks 5-9). Best-effort/fire-and-forget (an emit failure never fails the RPC), matching the task-4 hooks. **Boot wiring**: `cmd/kscore-server/policy.go` `startPolicy` builds `policyRuntime{Engine(+OPA/CEL/Builtin via WithEvaluator), Reports, Controls}` over `auditRuntime.Store`; nil audit store → PolicyService not registered (clients reach Unimplemented, secrets/events opt-out pattern). `main.go` calls `startPolicy` after `startAudit` + registers `PolicyService` after the events block (policy-eval auditor = `auditRT.FanOut`). **Task-4 CI contract test flipped**: `audit_emission_test.go`'s `TestAuditEmissionContract_PolicyEval_DeferredToTask9` placeholder replaced with `_EmitsAudit` — boots a `PolicyGRPCServer` with a recording auditor + real Builtin engine, asserts `EvaluatePolicy` emits one `policy.evaluate` entry; all 5 sensitive ops now have real contract coverage. **Tests** (`internal/controlplane` 76.1% incl. policy server, `-race` clean): `grpc_policy_server_test.go` 12 cases over a real SQLite-backed `audit.AuditStore` + real Engine — Builtin/OPA/CEL eval, deny+violations, set AllowedAll + per-member audit count, Get/List + offset pagination + bad-token, GetAuditLog + ListViolations(denied-only), GetComplianceReport counts, CRUD→Unimplemented, nil-components→Unavailable, structpb resource binding, error codes (empty-id/missing/disabled). `make lint` + `make docs-lint` clean.)_
13. **REST handlers** for v1.0 endpoints.
14. **`kscore-policy` v1.0 CLI** + **`kscore-audit` CLI**.
15. **Audit export formatters** (JSON, JSONL, CSV).
16. **Documentation** explicitly calls out audit-mode-only and the v1.0→v1.8 migration semantics.

## Acceptance criteria

- [ ] Every sensitive operation (auth, secrets, exec, state apply, policy eval) emits an `AuditEntry`. Verified by integration test.
- [ ] `kscore-audit log --since 1h --user alice` returns paginated entries.
- [ ] `kscore-audit export --format jsonl > out.jsonl` writes valid JSONL.
- [ ] Redaction strips configured patterns (e.g., `password=*`) from exported metadata.
- [ ] `kscore-policy eval my-policy.rego --input input.json` evaluates OPA policy and shows result + violations.
- [ ] `kscore-policy eval inline.cel --input input.json` evaluates CEL.
- [ ] `kscore-policy compliance --framework SOC2 --since 30d` returns ComplianceReport with rate, trends, top violations.
- [ ] `kscore-policy violations --severity critical --since 7d` lists violations.
- [ ] **v1.0 critical**: even when policies return `Allowed=false`, operations still proceed (audit-mode-only). Document in release notes loudly.
- [ ] CRUD RPCs return `Unimplemented` cleanly (clients can probe).
- [ ] Coverage >80% on `internal/policy`, `internal/audit`.

## Risks

- **Audit table growth** — retention policy MUST be set in v0.1 defaults; without it, disk fills. Monitor `kscore_audit_table_size_bytes`.
- **Redaction regex complexity** — overly broad patterns produce false positives. Document; recommend explicit pattern review before prod.
- **OPA default query assumption** — `data.<package>.allow` requires policies declare a package and `allow` rule. Provide templates.
- **CEL type errors at eval time** (dynamic typing) — `ValidatePolicy()` does syntax check but not full type validation. Test with realistic inputs.
- **Policy-set all-or-nothing semantics** (AND) — document; OR/threshold semantics for compliance-style sets in v1.5.
- **v1.0→v1.8 enforcement flip** is a behavior-changing release — release notes must call it out loudly.

## References

- PROJECT-DETAILS §4.12.
