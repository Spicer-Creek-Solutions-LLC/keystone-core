# Epic 14: Plugin / Module System (Starlark + Cosign + Filesystem Registry)

**Phase**: I • **Estimate**: 3 weeks • **Depends on**: 06, 08, 09, 10, 12 • **Blocks**: 15 (blueprint hooks may invoke modules)

## Goal

Salt-like extensibility on day 1. Sysadmins can author safe, sandboxed Starlark modules and ship them through a verified, reproducible distribution pipeline. **Strategic v0.1 scope decision**: Starlark-only + Cosign verification + filesystem registry — this delivers the *experience* of a real module system without the long tail of WASM/SumDB/cloud-backends.

## Scope (in)

### Manifest + lockfile

- `pkg/module/manifest/` — `Manifest{Name (namespaced vendor/pkg), Version (semver), Type (starlark in v1.0; wasm v1.1), Entrypoint, Description, Author, License, Capabilities, Limits, Dependencies}`.
- `CapabilityConfig` per capability: AllowedPaths, DeniedPaths, MaxFileSize, AllowedDomains, RateLimit, Timeout, AllowedCommands, AllowedSecretPaths, etc.
- Resource Limits: Timeout, Memory (string), CPU (float64).
- `LockFile{schemaVersion, modules map<name, LockedModule>}`; `LockedModule{version, hash}`.
- YAML serialization; round-trip stable.

### Capabilities (v1.0 — 9 core)

- `fs.read`, `fs.write` (path globs + denials + max_file_size)
- `http.get`, `http.post` (domain allowlists, request/response size limits, rate limits, timeouts)
- `exec` (command allowlists, working_dir, timeout)
- `secrets.read`, `secrets.write` (secret-path scoping)
- `kv` (in-process key-value)
- `log` (rate-limited)

### Verification

- `pkg/module/verify/`:
  - SHA-256 content addressing; CAS storage `~/.kscore/modules/<hash>/`.
  - Cosign signatures (RSA, ECDSA, Ed25519; KeyID-based key management).
  - Trust policy: TLS-trusted registry + Cosign signature in v1.0.

### Resolver

- `pkg/module/resolver/`:
  - Recursive dependency resolution against semver constraints (`>=1.0 <2.0`, `^1.5.0`, `~1.2.3`); prerelease-filter configurable.
  - DAG with cycle detection.
  - Conflict resolution: **Minimum Version Selection (MVS)** — Go modules pattern.
  - Module cache (content-addressed; CacheConfig: Dir, MaxSize, MaxAge, Readonly).
  - Lock file generation: topological sort + sorted by name.

### Registry (v1.0 — filesystem-backed)

- `pkg/module/registry/` + `cmd/kscore-registry/`.
- Go-mod-style HTTP endpoints:
  - `GET /<module>/@v/list`
  - `GET /<module>/@v/<ver>.info`
  - `GET /<module>/@v/<ver>.mod`
  - `GET /<module>/@v/<ver>.zip`
- Storage backend interface (`internal/registry/storage/`): `Get`, `Put`, `Delete`, `List`, `Exists`, `Stat`, `Health`. Filesystem backend in v1.0.

### Loader

- `pkg/module/loader/`:
  - `ModuleLoader` interface: `Load(path, options) → LoadResult`; `Execute(result, options) → ExecuteResult`; `LoadAndExecute`.
  - `LoadResult{Manifest, Runtime, VerificationResult, PolicyResult, CapabilityPolicyDecisions, RegisteredCapabilities, DeniedCapabilities, LoadDuration}`.
  - 7-step pipeline: parse → verify → policy check → capability policy → capability lock check (vs PreviousCapabilities) → runtime init → register granted capabilities only.
  - Module cache: load-time caching by path + content hash.
  - Load events: telemetry hooks at each pipeline phase.

### Runtime: Starlark

- `pkg/module/runtime/starlark/`:
  - `go.starlark.net`-backed.
  - Deterministic mode: `random()`, `time.now()` disabled by default (capability-gated).
  - Bytecode execution limits, recursion depth constraints, memory heap limits.

### Audit

- `pkg/module/audit/` — every capability invocation: `AuditEntry{Timestamp, Module, Version, Capability, Operation, Success, Duration, Details}`.
- `CapabilityInvoker` wraps the call → emit entry → integrate with §4.12 audit log.

### SDK

- `modules/sdk/starlark/` — host capability bindings as Starlark builtins; example modules.

### CLI

- `cmd/kscore-module`: `init`, `build`, `validate`, `resolve`, `verify`, `sign`, `test`, `publish`, `install`, `update`, `clean`, `tree`.
- Audit flags: `--audit-level`, `--audit-output`.

### Plugin discovery

- `pkg/plugin/`:
  - `Discovery.Discover()` scans `$PATH` for `kscore-*` binaries; caches.
  - `Executor.Execute()` runs them via `exec.Command`; stdin/stdout/stderr piping; context cancellation.
- This is what makes `kscorectl <foo>` dispatch to `kscore-foo` (Git-style plugin pattern).

## Scope (out / non-goals)

- **WASM runtime** (`tetratelabs/wazero`, WASI, instruction metering, memory bounds) — v1.1.
- Rust SDK, Go (TinyGo) SDK — v1.1.
- C++ SDK — v2.0.
- OCI registry backend — v1.1.
- S3/GCS/Azure storage backends — v1.1.
- `kscore-module mirror` air-gap — v1.1.
- SumDB transparency log — v1.2.
- Fine-grained capability model (per-syscall via seccomp/eBPF) — v1.2.
- Module vulnerability scanning + SBOM generation — v1.2.
- Federated module registries — v2.x.

## Design summary

See `PROJECT-DETAILS.md §4.18`.

## Tasks

1. **`Manifest` + `CapabilityConfig` + `LockFile`** types + YAML codec + tests.
   _(landed: new **`pkg/module/manifest/`** — **no new dep** (reuses the repo's direct `go.yaml.in/yaml/v3` + existing `pkg/semver`). `Manifest{Name,Version,Type,Entrypoint,Description,Author,License,Capabilities,Limits,Dependencies}`; single superset `CapabilityConfig` (`omitempty` per-capability fields — the epic's one-config design, not per-capability types); `Limits{Timeout,Memory,CPU}`; `LockFile{SchemaVersion,Modules}`/`LockedModule{Version,Hash}`. `MarshalManifest`/`UnmarshalManifest`/`MarshalLockFile`/`UnmarshalLockFile` (YAML); `ParseSize` (binary KB/MB/GB + KiB/MiB/GiB synonyms + bare bytes, fractional) + `ParseRate` (`<n>/<s|m|h>`) exported helpers. `Manifest.Validate()`: namespaced lowercase `vendor/pkg` name (regex — guards namespace squatting), `Version` via `semver.Parse`, `Type==starlark` (`wasm` explicitly rejected as v1.1-reserved), entrypoint required, capability keys ∈ the 9 core, per-capability size/rate/duration well-formed, `Limits` parse + `CPU>=0`, each dependency name namespaced + constraint shape via `semver.NewConstraint` (full resolution is task 6). `LockFile.Validate()` (schema==1, semver versions, `sha256:<64 hex>` hashes); lockfile marshals with deterministically sorted keys (yaml.v3) → byte-identical re-resolution = the reproducibility acceptance line. Verbatim §4.18 example manifest round-trips + validates; nil-safety; 97.2% cov (>80% `pkg/module/*` gate). `make lint`/`docs-lint` clean; default `go test ./... -race` unaffected (new isolated package; the pre-existing ROADMAP-logged `internal/statemgmt/stdlib/service` `-race` flake is not in this diff). Capability enforcement (T2/T3), verify/CAS (T4/T5), resolver/MVS (T6), registry (T8/T9), loader pipeline (T10), Starlark runtime (T11) NOT started.)_
2. **Capability registry + invoker + audit emission**.
   _(landed: new **`pkg/module/audit/`** + **`pkg/module/capability/`** — **no new dep** (stdlib + `internal/audit` + task-1 `pkg/module/manifest`). `pkg/module/audit`: module-domain `Entry{Timestamp,Module,Version,Capability,Operation,Success,Duration,Details}` (the §4.18 shape); `Auditor` interface (`Emit(ctx,Entry)`, fire-and-forget — the §4.11 "failure-to-log is a bug, not a request failure" contract) + `NoopAuditor`; **`StoreBridge`** adapts onto the §4.12 `internal/audit.Auditor` (maps `Entry`→`audit.NewAuditEntry`: `ResourceType=module`, `Action=module.<cap>`, `User=<module>@<ver>`, `Allowed=Success`, `Severity` Low / Medium-on-failure, `Metadata`={module,version,capability,operation,…Details} with reserved-key shadow guard); a construction error is logged + dropped, never propagated. `pkg/module/capability`: immutable **`Registry`** (granted-capability set, `NewRegistryFromManifest` projecting the validated manifest's capability keys — single-sourced via the newly-exported `manifest.KnownCapability`; unknown rejected defensively → `ErrUnknownCapability`; `Has`/sorted `List`; nil-safe = deny); **`Invoker.Invoke(ctx,cap,op,fn)`** — non-granted ⇒ fn NOT run, denied `Entry` emitted (`Operation=denied`, `Details.requested_operation`), `ErrCapabilityNotGranted` returned (the foundation for the "unauthorized exec/fs.write fails with audit entry" acceptance lines); granted ⇒ fn timed, outcome audited (`Success=err==nil`), error propagated unchanged. Clean one-way layering `capability → audit → internal/audit` (mirrors `pkg/api/cluster → internal/cluster`). audit 88.2% / capability 100% cov (the one uncovered audit branch is the unreachable-from-valid-input `NewAuditEntry` defensive error path); `make lint`/`docs-lint` clean; full `go test ./... -race` green (rc=0, no flake this run). The 9 capability *backends* + path/domain/command scoping (T3), verify/CAS (T4/T5), resolver/MVS (T6), registry server (T8/T9), loader pipeline that populates the Registry from the verified manifest (T10), Starlark runtime/builtins (T11/T12) NOT started.)_
3. **9 core capabilities** — pluggable backends (e.g., `SecretsStore`, `Logger`, `KVStore`, `HTTPClient`, `FSAccess`, `Executor`).
   _(landed: extends **`pkg/module/capability/`** — **no new dep** (`github.com/gobwas/glob` already a direct repo dep, used by policy/controlplane/targeting; rate-limiting is a tiny stdlib token bucket — `golang.org/x/time/rate` is deliberately not pulled, it is not in go.mod). Narrow injected host seams (`FSHost`/`HTTPHost`/`ExecHost`/`SecretsHost`/`Logger` + `Hosts` bundle) keep `pkg/module` dep-light; real `internal/secrets`/os-exec/net-http hosts wire at boot (deferred — the module boot-wiring ROADMAP item, like every Epic 14 host integration). The 9 scoped backends, each `New…(manifest.CapabilityConfig, host)` compiling scope once: **fs.read/fs.write** (`pathScope` = `glob.Compile(p,'/')` allow + deny globs, `path.Clean` first so `/a/../etc` can't escape `/a/**`; `MaxFileSize` via task-1 `ParseSize`), **http.get/http.post** (shared `httpCap`: domain-allowlist globs on `req.URL.Hostname()`, req/resp size caps with `io.LimitReader`, token-bucket `RateLimit`, per-call `Timeout` ctx), **exec** (command allowlist by exact + `filepath.Base`, `WorkingDir`, `Timeout` ctx), **secrets.read/secrets.write** (`SecretPaths` glob scope → `ErrSecretPathDenied`), **kv** (real in-process mutex map, optional key-count cap), **log** (token-bucket rate limit → `ErrRateLimited` on drop; nil host ⇒ slog). Typed sentinels `ErrPathDenied`/`ErrDomainDenied`/`ErrCommandDenied`/`ErrSecretPathDenied`/`ErrSizeExceeded`/`ErrRateLimited`/`ErrHostUnavailable` (host nil ⇒ fail-closed). `BuildCapabilities(m,Hosts)` assembles the granted+configured backend map (malformed scope fails the whole build — a module with a bad glob/size/rate must not load); the task-10 loader hands it to the runtime, task-12 exposes them as Starlark builtins. **Acceptance lines proven**: a dedicated test composes task-3 scoping with the task-2 `Invoker` — `fs.write` outside `Paths` → `ErrPathDenied` + audited failure entry; unauthorized `exec` (not granted) → fn-not-run + `ErrCapabilityNotGranted` + audited `denied` entry. `pkg/module/capability` 87.1% cov (>80% gate); `make lint`/`docs-lint` clean; full `go test ./... -race` green (rc=0). Fixed an in-scope FEATURES doc defect ("7 core capabilities" → "9", + source paths). Starlark builtins (T12), real host-backend boot wiring (deferred ROADMAP), verify/CAS (T4/T5), resolver (T6), registry (T8/T9), loader (T10), runtime (T11) NOT started. **Addendum: task 12 renamed the (then-unexported) `httpCap` → exported `HTTPCap` so the SDK can type-switch it — its 7 sibling backends were already exported; `NewHTTPGet`/`NewHTTPPost` now return `*HTTPCap`.**)_
4. **Cosign signature verifier** — accepts RSA/ECDSA/Ed25519; KeyID lookup against trust policy.
   _(landed: new **`pkg/module/verify/`** — **NO new dep** (decision: **Option C**, pure stdlib `crypto/*`, approved after a comparison table — `sigstore/cosign` pulls a Kubernetes-scale OCI/Rekor/Fulcio/TUF tree for capability that is entirely post-v1.0; `sigstore/sigstore`'s keyed path is a thin stdlib wrapper; the repo already does RSA/ECDSA/Ed25519 with stdlib in `internal/identity`). A cosign *keyed* detached-blob signature is exactly `verify(pubkey, sig, sha256(blob))` (ed25519 over the raw blob), so it is stdlib-only and cosign-`verify-blob`-compatible. `KeyID` = lowercase hex SHA-256 of the PKIX-DER public key (deterministic, rotation-friendly per §4.18). `TrustPolicy` (mutex map `KeyID→crypto.PublicKey`; `LoadTrustPolicy`/`AddKey`/`AddKeyPEM`/`KeyIDs`; v1.0 trust = a policy key **+** the TLS-trusted registry transport, the latter being the T8/T9 client's job). `Signature{KeyID,Algorithm,Value}` (`ecdsa-sha256`/`rsa-pkcs1v15-sha256`/`ed25519`). `Verifier.Verify(blob,sig)`: lookup→`ErrUnknownKeyID`; alg-tag vs key-type mismatch→`ErrUnsupportedAlgorithm`; ECDSA `VerifyASN1`/RSA `VerifyPKCS1v15` over `sha256(blob)`, Ed25519 over raw blob; fail→`ErrSignatureMismatch` (the "Cosign signature mismatch causes load to fail" acceptance line). Complementary `Sign(blob, crypto.Signer)` (key-type validated *before* KeyID so a bad signer is `ErrUnsupportedAlgorithm` not an opaque marshal error) produces cosign-verify-compatible detached sigs from a plain PKCS8/SEC1 PEM ("local.key") — feeds the T14 `kscore-module sign` CLI. Sentinels `ErrUnknownKeyID`/`ErrUnsupportedAlgorithm`/`ErrSignatureMismatch`/`ErrInvalidKey`. Tests: sign→verify round-trip all 3 algs, blob-tamper + sig-tamper → mismatch, unknown/wrong-key, alg mismatch, PEM load + garbage, KeyID stability + 2-key rotation, unsupported signer. `pkg/module/verify` 86.7% cov (>80% gate); `make lint`/`docs-lint`/trackerctl-mirror clean; full `go test ./... -race` green (rc=0). **Honest scope (ROADMAP-logged):** cosign keyless (Fulcio/Rekor), encrypted cosign keyfile interop, SumDB transparency → new `v1.x` ROADMAP entry "Module signing: cosign keyless / Rekor transparency / encrypted cosign keyfile interop" (+ release-order mirror). SHA-256 + CAS (T5), resolver (T6), registry/transport TLS (T8/T9), loader pipeline that calls `Verify` as step 2 (T10) NOT started. **Addendum (task 14): added `verify.MarshalSignature`/`UnmarshalSignature` — the JSON `{key_id,algorithm,value(base64)}` `.sig` on-disk/wire artifact (incomplete → `ErrInvalidKey`); feeds `kscore-module sign`/`verify` and the registry `.sig` transport.**)_
5. **SHA-256 hasher + CAS storage**.
   _(landed: new **`pkg/module/cas/`** — **no new dep** (stdlib `crypto/sha256`/`os`/`io`/`path/filepath`/`regexp`). Separate from `pkg/module/verify` (signatures) — the content-addressing half of the §4.18 pipeline; the task-10 loader composes both. `Hash(io.Reader)`/`HashBytes([]byte)` → the **`sha256:<64hex>`** form — exactly the task-1 `manifest.LockedModule.Hash` format, so a stored blob's hash drops straight into a lockfile and back; `ParseHash`/`FormatHash` + `ErrInvalidHash`. **`Store`** rooted at `<os.UserHomeDir()>/.kscore/modules` (the §4.18 `~/.kscore/modules` path; `./.kscore/modules` fallback when home unresolvable — the `internal/config/identity` overridable-default precedent); layout `<root>/<hex>/content`. `Put` streams through `sha256` (`io.MultiWriter`) into a temp file → `Sync` → atomic `Rename` (concurrent identical Puts cannot tear; racing-rename tolerated), idempotent (present → no rewrite). `PutExpected(r,want)` integrity-gates → `ErrHashMismatch` (the loader's "downloaded ZIP matches the lockfile" + reproducibility/verify acceptance lines), uncommitted on mismatch. `Get`(`ErrNotFound`)/`Has`/`Path`/`Delete`(idempotent)/`Verify` (re-hash → `ErrCorrupted` on on-disk tamper). gosec G301 → store dirs `0o750`. Tests (`t.TempDir`): well-known `sha256("hello")` vector, round-trip, idempotent Put, `PutExpected` good/mismatch/bad-hash (+ not-committed-on-mismatch), Get/Verify missing → `ErrNotFound`, `Verify` tamper → `ErrCorrupted`, 16-goroutine concurrent same-content Put, erroring-reader (no temp leak), `New` on a regular-file path, `Has`/`Delete` bad-hash + dir-at-blob-path, `DefaultRoot` via `t.Setenv`. `pkg/module/cas` 82.8% cov (>80% gate; the remaining gaps are contrived fsync/rename fault-injection paths). `make lint`/`docs-lint` clean; full `go test ./... -race` green (rc=0). The size/age/readonly cache *policy* on top of this store is **task 7** (`CacheConfig`); resolver (T6), registry (T8/T9), loader hash-check wiring (T10) NOT started. **Addendum: task 7 later added the additive `cas.Store.Entries()` + `cas.Entry` so the cache can enumerate blobs — see task 7.**)_
6. **Resolver** — semver constraints, DAG, cycle detection, MVS.
   _(landed: new **`pkg/module/resolver/`** — **no new dep** (reuses `pkg/semver` Parse/NewConstraint/Check/Compare/Sort + task-1 `manifest` + task-5 `cas` hash format). `Source` seam (`ListVersions`→`[]ModuleVersion{Version,Hash}`, `GetManifest`) — the filesystem registry implements it (T8/9); fake-tested here. `Config.AllowPrerelease` (the §4.18 configurable prerelease filter; default excludes prereleases). `Resolver.Resolve(ctx,root)`: accumulates **every** constraint imposed on each module across the DAG, **MVS** = the *lowest* available version satisfying all of them (Go-modules semantics — reproducible, no gratuitous upgrades; a tighter transitive constraint only pushes a selection **upward**, monotone ⇒ the fixpoint terminates); empty intersection → `ErrNoSatisfyingVersion`, missing module → `ErrUnknownModule`, bad constraint → `ErrInvalidConstraint`. Post-resolve DFS cycle check (back-edge → `ErrCycle` with the cycle path; self-loops too). `Resolution.LockFile()` → deterministic fully-pinned `manifest.LockFile` (each `Hash` re-validated via `cas.ParseHash`; task-1 `MarshalLockFile` sorts keys → byte-identical re-resolution = the reproducibility acceptance line); `Resolution.Order()` = topological (deps before dependents), root omitted. Tests (fake `Source`): MVS-picks-minimum-not-latest, transitive tightening reselects upward, no-satisfying / conflicting-ranges, cycle + self-loop, prerelease default-excluded vs `AllowPrerelease`, unknown module / bad constraint / nil root, lockfile validate+round-trip+determinism, `Order()` topo + root-omitted, malformed source hash rejected by `LockFile()`. `pkg/module/resolver` 93.4% cov (>80% gate). `make lint`/`docs-lint` clean; `pkg/module/resolver` green under full `go test ./... -race` (the run's `internal/cluster` `TestLeaderElector_SingleBecomesLeader` + `internal/statemgmt/stdlib/service` failures are the pre-existing full-suite `-race` contention flakes — both pass in isolation, ROADMAP-logged, not in this diff). Module cache/`CacheConfig` is **task 7**; registry `Source` impl (T8/9), loader (T10) NOT started.)_
7. **Module cache** with CacheConfig.
   _(landed: new **`pkg/module/cache/`** + a small additive **`cas.Store.Entries()`** (+`cas.Entry{Hash,Size,ModTime}`) so the cache enumerates blobs without duplicating the `<root>/<hex>/content` layout — **no new dep** (stdlib `os`/`time`/`sort`). `CacheConfig{Dir,MaxSize,MaxAge,Readonly}` + `validate` (MaxSize/MaxAge ≥ 0 → `ErrInvalidConfig`; `Dir=""`→`cas.DefaultRoot()`; zero MaxSize/MaxAge = that limit disabled, the `internal/config` retention convention). `Cache` wraps `*cas.Store`: `Put`/`PutExpected` (readonly→`ErrReadOnly`; else cas store then `enforce` MaxSize, never evicting the just-added hash), `Get` (MaxAge-stale by mtime + not-readonly → evict + `cas.ErrNotFound` miss; else serve and **touch mtime** via `os.Chtimes(Path)` so recency is **LRU**), `Prune` (drop stale then LRU-evict until ≤MaxSize; readonly→`(0,nil)` no-op), pass-throughs `Has`/`Verify`/`Path`/`Delete`(readonly→`ErrReadOnly`)/`Size`. **Decisions (stated):** mtime not atime as the recency clock (atime unreliable under `noatime`; `Get` touches mtime so it doubles as last-use); a single blob > MaxSize is still stored (CAS correctness > a hard cap that would break the loader), `enforce` just evicts everything else. `pkg/module/cache` 90.9% / `pkg/module/cas` 82.9% cov (both >80% gate; cas held with the new `Entries`). Tests (`t.TempDir`, mtime driven via `os.Chtimes`): round-trip+Size, MaxSize LRU eviction keeps-newest/never-just-added, `Get` mtime-touch flips LRU order, MaxAge stale → `Get` miss+evicted, `Prune` drops only stale (MaxSize kept continuously by enforce-on-Put, the LRU test's domain), Readonly Put/Delete→`ErrReadOnly`+Prune no-op+serves-stale, `PutExpected` mismatch→`cas.ErrHashMismatch`, oversize-single-blob kept, validate errors, zero-limits unbounded + CAS dedup, pass-throughs, `cas.Entries` enumerate/stat/skip-junk. `make lint`/`docs-lint` clean; full `go test ./... -race` only the pre-existing ROADMAP-logged `internal/statemgmt/stdlib/service` `-race` flake (isolated-pass, not in this diff). The loader's *load-time caching by path+hash* wiring is **task 10**; registry (T8/9) NOT started.)_
8. **Filesystem registry** — Go-mod HTTP endpoints; backend storage interface.
   _(landed: new **`internal/registry/storage/`** (the epic's explicit path) + **`pkg/module/registry/`** + an additive **`manifest.ValidModuleName`** (single-sources the namespaced-name regex shared with `Manifest.Validate`, the `KnownCapability` precedent) — **no new dep** (stdlib `net/http`/`os`/`io` + task-1/5/6 pkgs + `pkg/semver`). **`storage.Storage`** iface (`Get`/`Put`/`Delete`/`List`/`Exists`/`Stat`/`Health`) + **`Filesystem`** backend: traversal-safe `resolve` (rejects absolute/null and `..` that *escapes* root via `path.Clean`, while internal `a/b/../c` stays — defence-in-depth under-root assertion), atomic `Put` (temp→Sync→Rename, the cas precedent), `List` recursive+sorted skipping `.put-*`, `ErrNotExist`/`ErrInvalidKey`. **`Registry`** over a `Storage`: layout `<module>/<version>/{manifest.yaml,module.zip,info.json}` (`info.json`={Version,Time,Hash}, Hash=`cas.HashBytes(zip)`). `Publish` parses+`Validate`s the manifest (the namespaced-name check **is** the squatting guard, §4.18 risk), rejects empty zip / re-publish of an existing version (immutability → `ErrVersionExists`), bad manifest → `ErrInvalidModule`. **HTTP `Handler`** = Go module-proxy protocol via a single catch-all that splits the path on the `/@v/` infix (module names contain `/`): `list` (newline versions, text/plain), `<ver>.info` (**JSON `{Version,Time}` only** — Hash stays internal, §4.18 contract), `<ver>.mod` (manifest YAML), `<ver>.zip` (`application/zip`); unknown→404, malformed/non-namespaced/traversal-module→400. **`Registry` implements task-6 `resolver.Source`** (`ListVersions`→`[]ModuleVersion{Version,Hash}`, `GetManifest`) so the resolver resolves *directly* against it — proven by an end-to-end test (publish a graph → `resolver.Resolve` → pinned lockfile). One-way layering `registry → {manifest,cas,resolver,semver,internal/registry/storage}` (no cycle — resolver doesn't import registry). gosec G705 on the `list` write → scoped `//nolint` (semver-validated values, text/plain, not an HTML sink); test `get` returns `(int,string)` (bodyclose-clean, the kscore-cluster precedent). `pkg/module/registry` 86.4% (>80% gate); `internal/registry/storage` 79.4% (>70% `internal`-critical gate; the rest is contrived mid-stream fs-fault injection). `make lint`/`docs-lint` clean; full `go test ./... -race` only the pre-existing ROADMAP-logged `internal/statemgmt/stdlib/service` `-race` flake (isolated-pass, not in this diff). The standalone **`cmd/kscore-registry` server is task 9**; loader (T10) NOT started. **Addendum: task 9 added `POST /publish` (multipart) to this handler + `NewHandlerWithLimit`/`DefaultMaxUpload` — see task 9.** **Addendum (task 14): `Registry.Publish` now delegates to `PublishSigned(ctx,manifestYAML,zip,sig)` (sig optional → stored as `<mod>/<ver>/module.sig`) + `SignatureBytes(ctx,mod,ver)`; `publish` reads an optional `signature` multipart part; so the registry transports the detached `.sig` `kscore-module install` verifies against.**)_
9. **`cmd/kscore-registry`** server.
   _(landed: new **`cmd/kscore-registry/`** (cobra root, the kscore-migrate shape: `serve` + `version` subcommands, `pkg/version` template, `newCommand()` exposed for tests) + **`POST /publish`** added to the task-8 `pkg/module/registry` handler — **no new dep** (cobra/`pkg/version`/stdlib `net/http`/`mime/multipart`/`signal` all already in use). `serve` flags `--addr` (default `:8181`, the acceptance `localhost:8181`), `--dir` (`./registry-data`), `--max-upload` (default 64 MiB); builds `storage.NewFilesystem`→`registry.New`→`registry.NewHandlerWithLimit`→`http.Server`; `signal.NotifyContext(SIGTERM/Interrupt)` + bounded `Shutdown` (the `cmd/kscore-server` graceful-shutdown precedent). **Publish route** (`NewHandler`/`NewHandlerWithLimit`, `DefaultMaxUpload`=64 MiB): `multipart/form-data` `manifest`(YAML)+`module`(ZIP) → `Registry.Publish`; **decision (stated):** multipart not zip-embedded-manifest (reuses the two-arg `Registry.Publish` unchanged, no `archive/zip`, no API change — T14's `kscore-module publish` conforms); body bounded by `http.MaxBytesReader` (G120 false-positive → scoped `//nolint` with rationale). 201 Created / `ErrVersionExists`→409 / `ErrInvalidModule`→400 / missing-part/non-multipart/oversize→400. **Decision (stated):** v1.0 publish is **unauthenticated** — §4.18 trust = TLS-trusted registry transport + Cosign verification at load (the loader/T4's job), filesystem registry runs on a trusted boundary; logged as new `v1.x` ROADMAP entry "Module registry publish authentication" (+ release-order mirror, trackerctl green). Tests: `httptest` publish success→201 then read endpoints reflect it, re-publish→409, bad manifest→400, missing module part→400, non-multipart→400, oversize-body→400 (16-byte cap); `cmd` `newCommand` version/help smoke + a `serve` integration test (ephemeral port, multipart publish→201, GET list, ctx-cancel→graceful nil return) + bad-`--dir` fails fast. `pkg/module/registry` 85.8% (>80% gate); `cmd/kscore-registry` 76.2% (>40% CLI gate). `make lint`/`docs-lint`/trackerctl-mirror clean; full `go test ./... -race` only the pre-existing ROADMAP-logged `internal/statemgmt/stdlib/service` `-race` flake (isolated-pass, not in this diff). The `kscore-module publish` client is **task 14**; the loader fetching+verifying from this registry is **task 10**. **Addendum (task 14): added a `GET …/@v/<ver>.sig` endpoint (serves the stored detached signature as `application/json`, 404 for an unsigned module) + new `pkg/module/registry.Client` implementing task-6 `resolver.Source` over these Go-mod endpoints (+ `FetchZip`/`FetchSignature`/`Publish` multipart) — the HTTP integration glue `kscore-module resolve/install/publish` runs on.**)_
10. **`ModuleLoader` 7-step pipeline** — full implementation with tests for each phase.
   _(landed: new **`pkg/module/loader/`** — runtime-agnostic, all-seams; **no new dep** (stdlib `archive/zip` + tasks 1-5). A module artifact is a ZIP (`manifest.yaml` + entrypoint). Seams (the recurring dep-light pattern; production wiring deferred — see the new `gate-v1.0` "Module system boot wiring" ROADMAP entry): **`Runtime`/`Instance`** + `RuntimeRegistry` (Starlark impl = T11; loader fake-tested), **`PolicyChecker`** (nil ⇒ allow-all; prod = an `internal/policy.Engine` adapter at boot), `signatureVerifier` (task-4 `*verify.Verifier` + injected `TrustPolicy`), `capability.Hosts` (task-3), optional `*cas.Store` cache memo, `LoadObserver` (the §4.18 per-phase telemetry). `Load(ctx,path,opts)` runs the 7 steps: 1 **parse** (`readBundle`: zip→`manifest.yaml`→`UnmarshalManifest`+`Validate`+entrypoint) → 2 **verify** (`cas.HashBytes` + `verifier.Verify(blob,sig)` unless `SkipVerification`; nil verifier/sig ⇒ `ErrVerification`; a process-memoised verified hash short-circuits when a Cache is set, `VerificationResult.Cached`) → 3 **policy** (`CheckManifest` unless `SkipPolicyValidation`/nil → `ErrPolicyDenied`) → 4 **capability policy** (`CheckCapability` per requested cap → granted/`DeniedCapabilities`) → 5 **capability lock** (`opts.PreviousCapabilities` now-denied → `ErrCapabilityRevoked`) → 6 **runtime init** (`RuntimeRegistry.for_(m.Type)` → `Runtime.Init` with **only granted** caps built via `capability.BuildCapabilities(grantedManifest,hosts)`; missing → `ErrNoRuntime`) → 7 **register granted only** (`RegisteredCapabilities`/`DeniedCapabilities` sorted). `LoadResult` = the §4.18 shape; `Execute`/`LoadAndExecute` (the latter closes the instance). Sentinels `ErrManifest`/`ErrVerification`/`ErrPolicyDenied`/`ErrCapabilityRevoked`/`ErrNoRuntime`. Tests (fake Runtime/Instance/PolicyChecker, real `verify` keypair via `verify.Sign`, in-test module zips): happy path (all 7 phases, exact event order, runtime gets only granted backends incl. a real `*capability.Log`, Execute round-trip), skip-verify/skip-policy bypass, no-verifier/tamper → `ErrVerification`, manifest-deny → `ErrPolicyDenied`, cap-deny → not registered + runtime can't see it, cap-lock → `ErrCapabilityRevoked`, no-runtime/missing-file/not-a-zip/no-manifest → correct sentinel, cache short-circuits the verifier (call-count=1), `LoadAndExecute` closes. `pkg/module/loader` 91.5% cov (>80% gate). `make lint`/`docs-lint`/trackerctl-mirror clean; `pkg/module/loader` green under full `go test ./... -race` (the run's `internal/{cluster,controlplane,events,statemgmt/stdlib/service}` failures are pre-existing full-suite `-race` CPU-contention flakes — all pass in isolation, this task only adds the isolated `pkg/module/loader`). The Starlark `Runtime` impl is **task 11**; production seam wiring is the new `gate-v1.0` ROADMAP entry; the resolve/install flow is **task 14**.)_
11. **Starlark runtime** — sandboxed; deterministic mode.
   _(landed: new **`pkg/module/runtime/starlark/`** — **NEW dep `go.starlark.net@v0.0.0-20251222184526-15019ee33dea`** (approved via comparison table: the epic v0.1 decision mandates Starlark over WASM[v1.1]/Lua/Tengo; go.starlark.net is the only mature Starlark-in-Go, pure-Go, minimal transitive). Implements the task-10 `loader.Runtime`/`loader.Instance` (compile-time `var _ loader.Runtime`); registers for `manifest.TypeStarlark`. `Runtime.Init` compiles the entrypoint via `star.ExecFileOptions` under **strict `&syntax.FileOptions{}`** (no `while`, no top-level control, no global reassign, recursion-disabled — bounded + deterministic by construction); requires a callable `main` global (else `ErrNoMain`); compile failure → `ErrCompile`. **Deterministic by default**: the core universe excludes `lib/time`/`lib/random`, so `time`/`random` are simply unresolved (→ `ErrCompile`) unless a capability builtin exposes them — nothing to "disable". **`BuiltinProvider func(caps)→star.StringDict` seam** (task-11 default nil = deterministic core only; **task 12** fills the capability→builtin shims). `instance.Execute(ctx,input)`: JSON-ish `toStarlark`/`fromStarlark` converters (nil/bool/all int+uint sizes/float32-64/string/[]any/map[string]any ↔ Starlark; Tuple→[]any, non-string dict key → error, other → repr fallback); per-call `Thread` with `SetMaxExecutionSteps` + `OnMaxSteps`→`Cancel`, `Print`→`Logs`; **watchdog**: ctx-cancel/`Limits.Timeout`(task-1, else `Config.DefaultTimeout`) → `thread.Cancel` → `ErrStepLimit`/`ErrTimeout`/`context.Canceled`; `*star.EvalError` → `ErrExec` (with backtrace). Non-map return wrapped as `{"result":…}`. `Close()` idempotent no-op. **Honest scope:** a precise heap-bytes cap is not in go.starlark.net's public API — v1.0 bounds via step+wall-clock (+ no-`while`/recursion strictness); logged as new `v1.x` ROADMAP "Starlark hard heap-bytes cap" (+ release-order mirror). Tests: happy-path round-trip + `print`→logs, non-map-wrap, compile/no-main/non-callable-main, runtime-error→`ErrExec`, step-limit, timeout (steps huge so wall-clock wins), ctx-cancel, determinism (`time`/`random`→`ErrCompile`), `BuiltinProvider` seam (real `star.NewBuiltin` injected + provider-error aborts Init), manifest-`Limits.Timeout` honoured, all-types conversion + unsupported-input/tuple/non-string-key/func-fallback, **end-to-end through `loader.ModuleLoader`** (register runtime → `LoadAndExecute` a real zip). `pkg/module/runtime/starlark` 94.8% cov (>80% gate). `make lint`/`docs-lint`/trackerctl-mirror clean; `go mod tidy` clean; starlark green under full `go test ./... -race` (the run's `internal/{cluster,events}` failures are pre-existing full-suite `-race` CPU-contention flakes — isolated-pass; this task only adds the isolated starlark pkg). The capability→builtin shims are **task 12**; runtime registration into kscore-module is **task 14** (+ the `gate-v1.0` "Module system boot wiring" entry). **Addendum: task 12 exported the converters `toStarlark`/`fromStarlark` → `ToValue`/`FromValue` so the SDK reuses them (the `KnownCapability` DRY precedent).**)_
12. **Starlark capability builtins** — Go shims wrapping Capability backends.
   _(landed: new **`modules/sdk/starlark/`** (the §4.18 SDK path; `package starlarksdk`) — **no new dep**. Each granted task-3 backend is exposed as a Starlark namespace object (`nsValue`: `star.Value`+`HasAttrs`, frozen, sorted `AttrNames`): **`fs`**(.read/.write), **`http`**(.get/.post), **`exec`**(.run), **`secrets`**(.read/.write), **`kv`**(.get/.set/.delete), **`log`**(.debug/info/warn/error) — only granted families are added (a non-granted namespace is simply absent → Starlark resolve/NameError → the natural deny). Every method routes through the task-2 `capability.Invoker` (`guard`→`inv.Invoke` = grant-gate + audit `Entry`); a scoping/denial error becomes a Starlark error that aborts the module (intended security behaviour; the failure is still audited). `BuildStringDict(caps,inv)` type-switches the `capability.BuildCapabilities` output onto namespaces (fs.read+fs.write→`fs`, http.get/post→`http`, secrets.*→`secrets`); `Provider(inv) srt.BuiltinProvider` plugs straight into the task-11 `starlark.Config{Builtins}`. Args via `star.UnpackArgs`; values via the **newly-exported `srt.ToValue`/`srt.FromValue`** (secrets `map[string]any`↔dict). **Decisions:** namespace objects (dotted names aren't valid Starlark idents); capability calls use `context.Background()` (the task-11 thread watchdog already bounds the whole execution — new `v1.x` ROADMAP "Per-capability-call context propagation in the Starlark SDK" + release-order mirror); per-module Invoker wiring is task 14 / the `gate-v1.0` "Module system boot wiring" entry (this task tested with the Invoker wired directly). **In-scope fix:** `capability.httpCap` was the only unexported backend type (its 7 siblings exported) → renamed **`HTTPCap`** so the SDK (any consumer) can type-switch it; `NewHTTPGet`/`NewHTTPPost` now return `*HTTPCap` (internal `newHTTPCap` ctor unchanged). Tests: full end-to-end through a real task-11 `starlark.Runtime` exercising every namespace (kv/log/fs/secrets/http-get+post/exec) + audit-entry assertions, scoping-denial→`srt.ErrExec`+audited-failure, non-granted-namespace absent, `dir(kv)` (`AttrNames`), attr-miss, arg/unpack/non-string-list/non-dict error paths, `BuildStringDict` unknown-value/nil-invoker/empty. `modules/sdk/starlark` 87.6% cov (>80% gate). `make lint`/`docs-lint`/trackerctl-mirror clean; full `go test ./... -race` **rc=0 (fully green, no flakes this run)**. The capability→builtin shims are complete; **task 13** = `pkg/plugin` discovery + `kscorectl` dispatch; **task 14** wires loader+runtime+SDK into `kscore-module`.)_
13. **`pkg/plugin`** Discovery + Executor; `kscorectl` integration.
   _(landed: new **`pkg/plugin/`** + `cmd/kscorectl` Git/kubectl-style dispatch — **no new dep** (stdlib `os`/`os/exec`/`path/filepath`/`sync` + cobra already in use). **`Discovery`** (`New(prefix)`, default `kscore-`) scans `$PATH`, collects executable `<prefix>*` files → `Plugin{Name,Path}`, **first-PATH-match wins** (shell dedup), skips dirs/non-exec/empty-name, mutex-cached + `Refresh()`; `Lookup(name)`. **`Executor.Execute(ctx,p,args,stdin,stdout,stderr)`** = `exec.CommandContext` with stdio wired; returns the child's exit code (`*exec.ExitError.ExitCode()`; `-1` on spawn failure), `ErrPluginNotFound` on empty path; **`cmd.WaitDelay = 3s`** so a ctx-cancelled plugin returns promptly instead of hanging on stdout/stderr copier goroutines a grandchild inherited (the standard os/exec + CommandContext caveat — found via a failing 30s `sleep` ctx-cancel test). **`Dispatch(ctx,root,args,d,e,stdio)`** = the cobra glue (git-plugin model: `kscorectl <name> …`, name = first arg): not handled if args empty / leading flag (`--help`/`--version`/`--config` → cobra) / a cobra builtin (`help`/`completion`/`__complete*`) / a **registered** subcommand (`isRegistered` scans `root.Commands()` + aliases — never shadows `exec`/`state`/…); else `d.Lookup(name)` → if found `e.Execute(name's-tail-args)` (handled+code), else not handled (cobra emits "unknown command"). `cmd/kscorectl` `main` refactored to a testable `runCLI(args,stdio) int` (the kscore-migrate/registry precedent) — `signal.NotifyContext(SIGINT/SIGTERM)` ctx, plugin dispatch tried before `root.Execute()`. **Decisions:** git-plugin convention (plugin name is the first arg; kscorectl global flags follow it); never shadow registered/builtin commands; first-PATH-match wins; `CommandContext` default-kill + `WaitDelay` (no graceful child-signal — not worth complexity for v1.0). Tests: `Discovery` over a fake `$PATH` (prefix-strip, first-wins, skip dir/non-exec/wrong-prefix/empty-name, cache+`Refresh`); `Executor` real scripts (args, stdin→stdout pipe, non-zero exit forwarded, spawn-failure→-1, empty-path err, ctx-cancel kills a `sleep 30` within budget); `Dispatch` table (unknown-with-plugin→handled+args+code, registered subcommands→not handled, unknown-no-plugin→not handled, root-flag/`help`/empty→not handled) with injected fake Executor; `runCLI` (`--help`→0, unknown-no-plugin→1, real `kscore-demo` PATH plugin→exit-5 forwarded + stdout). `pkg/plugin` 97.4% (>80% gate); `cmd/kscorectl` 89.5% (>40% CLI gate). `make lint`/`docs-lint` clean; full `go test ./... -race` **rc=0 (fully green, no flakes this run)**. The mechanism is live — any `kscore-*` on `$PATH` is a `kscorectl` subcommand; once **task 14** ships `kscore-module`, `kscorectl module …` dispatches automatically (no further wiring).)_
14. **`cmd/kscore-module`** CLI with all listed subcommands.
   _(landed: new **`internal/cli/module/`** (cobra, `Deps`-injected — the kscore-policy precedent) + **`cmd/kscore-module/`** thin wrapper — **no new dep**. All 12 subcommands: **init** (scaffold dir: valid `manifest.yaml`+`main.star`; namespaced-name + non-empty-dir guards), **validate** (`manifest.Validate`), **build** (`archive/zip` the dir → ZIP, skips `.sig`/`.lock`/the output), **sign** (`--key` PKCS8/SEC1/PKCS1 PEM → `verify.Sign` → detached `<zip>.sig` via the new codec), **verify** (trust-policy from `--key` pub PEM + `.sig` → `verify.Verifier.Verify`), **resolve** (`resolver.Resolve` against the registry client; `-o module.lock`), **publish** (read built ZIP, extract `manifest.yaml`, optional `<zip>.sig` → `Client.Publish` multipart), **install** (`vendor/pkg@ver` or `[dir]`: resolve → per dep `Client.FetchZip` → `cas.PutExpected` hash-gate → if `--key` fetch+`Verify` the signature → deterministic `module.lock`; reproducible), **update** (fresh re-resolve → rewrite lock), **tree** (recursive resolved DAG via `GetManifest`, cycle-guarded), **test** (→ the `TestRunner` seam; default = `ErrTestFrameworkPending`, task 15 injects the real `pkg/module/testing` runner — `--audit-level`/`--audit-output` threaded as `AuditOptions`), **clean** (clear the cas cache root). **Supporting infra added (the integration glue):** `pkg/module/registry.Client` (HTTP, implements task-6 `resolver.Source` over the task-9 Go-mod endpoints + `FetchZip`/`FetchSignature`/`Publish`; falls back to hashing the fetched ZIP when the server doesn't expose Hash); `verify.MarshalSignature`/`UnmarshalSignature` (the JSON `.sig` artifact); registry `PublishSigned` + an optional `signature` multipart part + a `GET …/@v/<ver>.sig` endpoint + `SignatureBytes` (so `install` verifies signatures fetched *from* the registry). **Decisions (as planned):** `test` is a seam to task 15 (acceptance is epic-level); signatures transported via the registry; git-plugin reachable as `kscorectl module …` (task 13). Tests: a full **init→validate→build→sign→verify→publish→install** end-to-end through a real `httptest` registry (Task-8/9 handler), **reproducible install** (byte-identical re-`module.lock`), tamper→verify-fails (the "signature mismatch causes load to fail" acceptance line), init guards, `test` seam (default-pending + injected-runner pass/fail); `verify` codec round-trip+errors; `registry.Client` publish/fetch/resolve/signature + unsigned-absence. gosec G703/G705 on operator-supplied `-o` path + JSON-sig write → scoped `//nolint` w/ rationale (the cas/registry precedent); staticcheck S1016 → `sigJSON(sig)`/`Signature(s)` conversions. `internal/cli/module` 68.9% (>40% CLI gate); `pkg/module/registry` 80.9%, `pkg/module/verify` 87.6% (>80%). `make lint`/`docs-lint`/trackerctl-mirror clean; full `go test ./... -race` **rc=0 (fully green)**. **Acceptance lines satisfied:** init scaffolds, build ZIPs, sign produces a Cosign-compatible sig, publish uploads+indexes, install resolves+downloads+verifies sigs+hashes+lockfile, sig-mismatch fails, `kscorectl module` dispatches (task 13); the fs.write/exec-denied+audit lines were already proven (tasks 3/12); `kscore-module test runs Starlark unit tests` lands with **task 15**. Example modules = task 16; e2e fake-registry integration = task 17. Addenda below. **Addendum (task 15): the `TestRunner` seam is now filled — `cmd/kscore-module` refactored to a testable `run(args,stdout,stderr) int` injecting `module.Deps{TestRunner: pkg/module/testing adapter}`; `internal/cli/module` stays dep-light (default stays `ErrTestFrameworkPending` for the injection seam + fast CLI tests). `kscore-module test runs Starlark unit tests` acceptance now satisfied.**)_
15. **Module test framework** in `pkg/module/testing/` — Starlark unit-test runner.
   _(landed: new **`pkg/module/testing/`** (path per the epic; **`package moduletest`** so importers don't shadow stdlib `testing` — the `pkg/module/audit`→`maudit` aliasing precedent) — **no new dep** (reuses `go.starlark.net` + tasks 1/2/3/11/12 `manifest`/`capability`/`runtime/starlark`/`modules/sdk/starlark`). **Fills the task-14 `internal/cli/module.TestRunner` seam** (`kscore-module test` no longer returns `ErrTestFrameworkPending`). `Run(ctx,moduleDir,Options)→(*Report,error)`: reads+`Validate`s `manifest.yaml`, builds the capability backends/`Invoker`/audit, execs the manifest entrypoint once (strict `&syntax.FileOptions{}` — runtime parity) → module globals, then for each `*_test.star` (sorted) execs it with predeclared = **SDK capability builtins ∪ module globals ∪ `assert`** and runs every callable `test_*` global. Each test runs on its own bounded thread mirroring the task-11 watchdog (`SetMaxExecutionSteps` + ctx/`Limits.Timeout` cancel → `ErrStepLimit`/`ErrTimeout`/`ctx.Err`); `print` captured per-test; an `*EvalError` → failed `Result` (backtrace). **`assert`** namespace (frozen, `HasAttrs`, local — keeps the SDK internal): `eq/ne/true/false/contains/fails/fail`; a failed assert is a Starlark error that aborts that test. Single-entrypoint model — no `load()`; module funcs reached via the merged predeclared. **Hosts decision:** test defaults = os-backed `fs` (the capability layer still enforces the manifest's `AllowedPaths`/`MaxFileSize`, so "fs.write outside scope fails + audited" is demonstrable from a test) + discard `Logger`; `http`/`exec`/`secrets` nil → fail-closed + audited (record/replay test hosts deferred → ROADMAP). `AuditOptions{Level,Output}`: `Level` ""/all vs failure/deny filter, `Output` ""→Noop / stdout / stderr / file (JSON lines, append). Infra failure (bad manifest/entrypoint/cap-build) → hard error; a broken `*_test.star` → one failed `Result` (others still run); Run returns `(report,nil)` whenever the harness ran (caller derives exit from `report.Failed`). **Seam wired at the boot layer:** `cmd/kscore-module` refactored to a testable `run(args,stdout,stderr) int` (the kscore-migrate/registry precedent) injecting `module.Deps{TestRunner: moduletest-adapter}` — `internal/cli/module` stays dep-light (the established Epic-14 production-wiring pattern; the seam/`ErrTestFrameworkPending` default stays for injection + fast CLI tests). Tests: pass/fail/mixed + non-callable-`test_*` skipped, no-test-files→0/0, isolated test-file compile error, `print`→Logs + duration, hard errors (missing/invalid manifest, missing/broken entrypoint, unknown cap), step-limit, wall-clock + manifest-`Limits.Timeout`, white-box pre-cancelled-ctx `runOne`, kv+log capabilities, fs.write scope-denied + **audit JSON asserted** (`failure` level), every `assert` helper + namespace value, `newAuditor` level/output/file/filter/open-error, `cmd` all-pass→0 / failing→1 / infra-err→1 / `--help`→0 + adapter. `pkg/module/testing` 89.4% (>80% gate); `cmd/kscore-module` 92.3% (>40% CLI gate, up from no test files). `make lint`/`docs-lint`/trackerctl-mirror clean; full `go test ./... -race` green (only the pre-existing ROADMAP-logged `internal/{cluster,controlplane,events,statemgmt/stdlib/service}` full-suite `-race` CPU-contention flakes, isolated-pass — this task only adds the isolated `pkg/module/testing` + a `cmd` test). **Acceptance:** `kscore-module test runs Starlark unit tests` now satisfied. **Honest scope (ROADMAP-logged `v1.x`):** injectable record/replay http/exec/secrets test hosts; multi-file module tests via Starlark `load()`. Example modules = task 16; e2e fake-registry = task 17.)_
16. **3 example modules** in `modules/examples/` to validate the full author UX (init → build → sign → publish → install → execute).
   _(landed: **7 example modules** under `modules/examples/` — **no new dep** (data + a Go driver test reusing tasks 8/10–12/15). The epic spec is "3"; the user directed an expansion to **7** to cover real-world use cases (flagged per AGENTS.md §3 before expanding). Each module = `manifest.yaml` + `main.star` + `<leaf>_test.star` + `README.md`, spanning the v1.0 capability set: **`keystone/hello`** (zero-capability deterministic), **`keystone/kvcache`** (`kv`+`log`, fully testable under default hosts), **`keystone/fsreport`** (`fs.read`/`fs.write` scoped `paths`/`denied_paths`/`max_file_size` — the security showcase; module has no path checks, the runtime denies+audits), **`keystone/httpfetch`** (`http.get` domain/size/timeout scoped), **`keystone/cmdrun`** (`exec` command-allowlist + `working_dir`), **`keystone/secretsync`** (`secrets.read` `app/source/*` + independent `secrets.write` `app/dest/*` — least-privilege), **`keystone/opsbundle`** (`kv`+`log` + `dependencies:` on httpfetch & cmdrun — the resolve/tree/lockfile distribution-pinning UX; runtime `load()` of deps is the post-v1.0 item). **Honest design:** `http`/`exec`/`secrets` have no host wired under `kscore-module test` (the task-15 decision; record/replay test hosts are the existing task-15 `v1.x` ROADMAP entry — referenced from the example READMEs), so those modules' `*_test.star` assert pure logic + **fail-closed + audited**; their live host-backed paths (allow + deny) are proven in the Go driver with injected fake hosts. `modules/examples/examples_test.go` (`package examples_test`): table over all 7 → `manifest.Validate`; the **real `kscore-module` CLI** via `module.NewCommand` (`validate` → `build` ZIP → `test`, the test seam injected like `cmd/kscore-module`); `moduletest.Run` (Failed==0, Passed>0); per-capability **entrypoint execution** through the task-11 runtime + task-12 SDK with fake `HTTPHost`/`ExecHost`/`SecretsHost`/os-`FSHost` (asserting output + audited successes + out-of-scope/denied-domain/denied-command/cross-scope-secret rejection); **opsbundle dependency resolution** against an in-process `storage`+`registry` (publish httpfetch+cmdrun → `resolver.Resolve` → lockfile pins all three @1.0.0 → **reproducible** byte-identical re-resolve). `modules/examples/README.md` index + per-module READMEs document the canonical `init→…→install` author flow. **Scope boundary (stated):** the sign/publish/install registry round-trip stays with the task-14 e2e + the task-17 generic fake-registry e2e — example 7 is the dependency-UX fixture, not a replacement for task 17. No new `pkg/module/*` code → the >80% gate is unaffected; `examples_test.go` exercises runtime/SDK/moduletest/registry/resolver with no coverage regression. `make lint`/`docs-lint`/trackerctl-mirror clean; full `go test ./... -race` only the pre-existing ROADMAP-logged `internal/statemgmt/stdlib/service` `-race` flake (isolated-pass; this task adds only the isolated `modules/examples`). Strengthens the already-`[x]` "validate the full author UX" acceptance; e2e fake-registry integration = task 17 (the last Epic-14 task).)_
17. **Integration test**: end-to-end module flow with fake registry server.
   _(landed: new **`test/e2e/module/`** (`//go:build integration`, `package modulee2e`, run via `make test-integration`) + `README.md` — **no new dep** (reuses tasks 8/9/10–12/15 + `pkg/semver`). The **canonical Epic-14 e2e**: the real `kscore-module` CLI constructed exactly as `cmd/kscore-module` (registry HTTP client + task-15 `moduletest` runner injected) driven against a **fake registry server** = the task-9 handler over `storage.NewFilesystem(t.TempDir())` served by `httptest` (real Go-mod wire protocol on a real `net/http` server; `t.Setenv("HOME", t.TempDir())` isolates the cas root). Four scenarios: (1) **full lifecycle** `init→validate→test→build→sign→verify→publish→install --key` → **reproducible** byte-identical re-`module.lock` → **execute the distributed artifact** (fetch zip via `registry.Client` → unzip → run through the task-11 runtime + task-12 SDK — the "execute" UX step no prior test did end-to-end); (2) **dependency graph over HTTP** — a root `dependencies:` on two published leaves → CLI `resolve`/`tree`/`install` against the HTTP registry → lockfile pins all + reproducible re-resolve (the task-16 opsbundle test resolves in-process; this is CLI-over-HTTP); (3) **integrity failures** — wrong `--key` (sig mismatch), `--key` vs unsigned, and a **signed** module whose stored ZIP is tampered post-publish (sig over original bytes → `install --key` rejects it) — each fails and **writes no `module.lock`**; the unsigned-tamper case is deliberately omitted (a registry serving self-consistent tampered bytes+hash can't be caught by the hash gate alone — exactly why signing exists; documented in the README "Scope — honest"); (4) **registry Go-mod protocol conformance** — `.info` exposes only `{Version,Time}` (Hash internal, §4.18), `.sig` 200 signed / 404 unsigned, re-publish → conflict. **Honest scope (README + already ROADMAP-logged):** real network/OCI/SumDB registry backends are post-v1.0; live http/exec/secrets test hosts are the task-15 `v1.x` entry (examples cover them with injected fakes). `make lint` clean **and** `golangci-lint --build-tags=integration` clean (the Epic-13 build-tagged-lint precedent — integration files aren't default-tag type-checked); `make docs-lint`/trackerctl-mirror clean; `make test-integration` green + stable; default `go test ./...` excludes it (build-tagged — "no packages to test"), so the pre-existing `internal/statemgmt/stdlib/service` `-race` flake is irrelevant. All 12 acceptance criteria are now backed by an end-to-end suite. **This closes Epic 14.**)_

## Acceptance criteria

- [x] `kscore-module init my-module` scaffolds a Starlark module with manifest.
- [x] `kscore-module build` packages module as ZIP.
- [x] `kscore-module sign --key local.key` produces Cosign signature.
- [x] `kscore-module publish --registry http://localhost:8181` uploads + indexes.
- [x] `kscore-module install vendor/example@v1.0.0` resolves dependencies, downloads ZIPs, verifies signatures + hashes, populates lockfile.
- [x] `kscore-module test` runs Starlark unit tests.
- [x] Module attempting `fs.write` outside allowed paths fails with clear error + audit entry.
- [x] Module attempting unauthorized `exec` fails with audit entry.
- [x] Re-running install with same lockfile produces identical resolution (reproducible).
- [x] Cosign signature mismatch causes load to fail.
- [x] `kscorectl module` dispatches correctly to `kscore-module`.
- [x] Coverage >80% on `pkg/module/*`.

## Risks

- **Starlark sandbox escape** — defense in depth: runtime limits + capability scoping + policy + audit. Regular security review.
- **Capability creep** — defaults are permissive for ergonomics; production policies tighten.
- **Signature key rotation** — multiple trusted keys + transparency log (v1.2) for tamper detection.
- **Determinism violations** — Starlark `random()` and `time.now()` disabled by default; capability gate.
- **Module version conflicts** — MVS guarantees consistent resolution; lock file pins exact versions.
- **Lock-file drift** — validate on every load; CI must check.
- **Registry availability (SPoF)** — local cache deduplication mitigates; v1.1 mirror command.
- **Namespace squatting** — registry enforces namespaced names; validation on publish.

## References

- PROJECT-DETAILS §4.18.
