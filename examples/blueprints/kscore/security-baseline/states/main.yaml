# Security Baseline Main State File
# Entry point for security hardening

metadata:
  name: security-main
  description: Security baseline entry point
  version: "1.0"

states:
  security_baseline_marker:
    module: file
    state: managed
    name: /etc/kscore/security-baseline.enabled
    contents: "Keystone Core security baseline applied."
    mode: "0644"
