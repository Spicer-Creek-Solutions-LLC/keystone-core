metadata:
  name: kubernetes-operator-main
  description: Placeholder state for the kubernetes-operator blueprint scaffold
  version: "0.1"

states:
  kubernetes-operator_blueprint_dir:
    module: file
    state: directory
    name: /var/lib/keystone-core/blueprints

  kubernetes-operator_blueprint_placeholder:
    module: file
    state: managed
    name: /var/lib/keystone-core/blueprints/kubernetes-operator.txt
    contents: "Placeholder for kscore/kubernetes-operator blueprint."
    require:
      - kubernetes-operator_blueprint_dir
