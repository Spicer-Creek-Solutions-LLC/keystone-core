mode: development

server:
  host: 127.0.0.1
  grpcport: 5397
  httpport: 8080
  tls:
    enabled: false  # dev fixture opts out per Epic 09 task 13

logging:
  level: info
  format: json

storage:
  driver: sqlite
  dsn: ./data/keystone.db
